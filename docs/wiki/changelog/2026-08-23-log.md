# 2026-08-23 维护日志

## 2026-08-23-09

- **CI 三平台调通**：首轮 develop push 触发后连续暴露并修复三处跨平台问题——`scene_ops.cpp` 缺 `<algorithm>`（Linux libstdc++ 无 MSVC STL 的传递包含）；macOS universal 交叉编译撞 homebrew 仅 arm64 的 OpenSSL（FetchDependencies 统一 `CMAKE_DISABLE_FIND_PACKAGE_OpenSSL`，MCP 仅明文 localhost 用不到 TLS）+ Linux clang++-18 找不到 `-lgcc_s`（workflow 兜底创建符号链接）；gdextension Linux 条目文件名与实际部署名不一致被完整性校验拦截（`godot-autopilot.so` → `libgodot-autopilot.so`）。最终 run 32610057563 三平台全绿（ubuntu 11m / macos 15m universal 双架构 / windows 10m）
- **遗留观察**：sccache ghac 后端 `cache_write_errors` 全量失败（1204/1204），缓存从未生效致每次全量重编约 11 分钟，疑似 mozilla-actions/sccache-action v0.0.8 与新版 GHA cache API 兼容问题，待升级 action 验证；各 action Node 20 deprecation 警告待择机升级

## 2026-08-23-01

- **CI/Release 工作流落地**：新增 `.github/workflows/ci.yml`（develop push/PR 触发，三平台 ubuntu/macos/windows Debug 编译 + L1 测试 `ctest --preset debug -E "^gda_runner_"`，sccache + `_deps` 缓存）与 `.github/workflows/release.yml`（tag `v*` 触发：validate 校验 tag 与 VERSION 一致 → 三平台 Release 构建上传库 artifact → package 合并为 `addons.zip` 经 softprops/action-gh-release 发布）
- **build.py 扩展**：新增 `--package --libs-dir <dir>`（递归收集三平台库合并部署后打包，缺失任一即报错；须与 `--package` 同用）；gdextension macOS 条目 `macos.{debug,release}.x86_64` → `macos.{debug,release}.universal`
- **CMakePresets.json**：debug/release 预设加 `CMAKE_OSX_ARCHITECTURES=x86_64;arm64`（仅 Apple 生效，CI ARM runner 产出 universal 双架构库，Godot 4.4+ 支持显式 universal 标签）
- **版本号**：根 `VERSION` 0.2.0 → **0.2.1**
- **验证**：workflow YAML 解析通过；模拟三平台 artifact 目录实测 `--package --libs-dir` 合并打包，zip 结构 `addons/godot-autopilot/{gdextension,dll,so,dylib}` 与 universal 条目正确；validate 脚本逻辑 WSL bash 实测通过；本地 Debug 全量构建 + 部署成功、L1 71/71 通过（`-E "^gda_runner_"` 排除 L2 因 CI 无 GODOT_PATH 必败的 6 个用例）
- **文档同步**：AGENTS.md（构建节补命令与 CI/Release 说明、知识局限「无 CI」改「CI 不跑 L2」）、build.md（审计日期 08-23、行号重核至 build.py 239 行、新增「CI 与 Release」章节）、overview.md/conventions.md 清除「无 CI」表述
