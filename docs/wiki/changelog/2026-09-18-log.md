# 2026-09-18 维护日志

## 2026/09/18 16:35

- **README 拆分批次补记（4697420，此前未被 09-17 日志收录）**：`README_zh.md` 删除（179 行），`README.md` 改为中文为主（261 行），新增英文版 `README.en.md`（82 行）与 `assets/demo-001.gif`。工具口径改为 badge `tools-385+`（`README.md:8` / `README.en.md:8`）+“385+ 个工具 / Over 385 tools”（`README.md:26` / `README.en.md:26`，域工具口径，以 `search_tools` 为准）；前提改为“Godot 4.7+”（`:5,53`）；构建章节收敛为指向 wiki 与 `tests/README.md` 的一句话（`README.md:70`）。
- **代码注释修正（零行为影响）**：`src/tools/register_all.cpp:363` 的 `call_tool` 描述由“385 non-meta（384 domain + system_status）”改为“386 non-meta（385 domain + system_status）”，与 385 域工具实测对齐；该字符串全仓唯一引用，无测试断言。
- **wiki 同步 6 页**：`overview.md`（事实来源与工具总数段改引新版 README、前提 4.3+ 宽松下界改为 4.7+ 三方一致、`resource` 加 `README.en.md`）、`build.md`（`CMakeLists.txt` 169→173 行、事实来源与文档一致性段改引新版 README 行号）、`tests.md`（`GDA_ENABLE_TESTS` 开关 `CMakeLists.txt:164`→`:168`）、`modules/core.md`（`query_recent(50)` 消费方 `resource_handlers.cpp:470`→`:527`、README 对照 3 行改引新版行号并消解“文档间冲突”、审计日期头补齐 09-16 改动的同步）、`modules/entry_runtime.md`（`game_bridge_eval.cpp` 509→799 行）、`example.md`（“4.3+ 宽松下界”消除）。改动页 `timestamp` 同步 `2026-09-18T16:35:00+08:00`，带日期头的页审计日期同步 09-18。
- **审计方法与未动项**：3 组子代理并行静态核算（`GDA_TOOL_CLASS(` 325 / `GDA_TOOL_CLASS_SIDE(` 60 / 385 域 / 7 元 + `system_status` / L1 243 / config 25 / skill 30 个 .md + 8 册 / 客户端 20）与文档互核一致；子代理 4 处行号结论经主代理实测纠正为误报（`main.cpp` 326 行、`gda_protocol.hpp` 53 行、`game_bridge.hpp` 79 行、`build.py` 239 行均为文档原值正确）。`conventions.md` 添加工具流程与 AGENTS.md 三步口径一致（第 4 项为构建约束补充，无“三步”字样系误报）；`AGENTS.md` 全段数值复核无误，本次不动。`modules/support.md` 的 `timestamp` 停留在 09-17T00:58（09-17 当日改动未同步）列为遗留，本次未受影响按规则不动。
- **时间真实性**：本节时间为真实时间 2026-09-18 16:35 +08:00；历史日志零改写。

## 2026/09/18 19:30

- **计划驱动执行 E2E 优化批次（端到端报告 F1–F8 → P0–P2/I1–I8，共 8 个 feature 分支、T00–T09，编码全委派子代理、构建测试由主代理执行，未提交）**：新增 6 个域工具——`verify_scene_saved`（只读，Editor）、`patch_script`（SIDE/WritesFile，Scripts）、`sample_game_property`/`collect_game_evidence`/`validate_game_ui_layout`（只读，Game）、`build_nodes_from_spec`（SIDE/None，Scene，与 `fill_tilemap_rect` 同款仅宏前缀排除）。域工具 385→**391**（非 SIDE 325→**329**、SIDE 60→**62**），catalog/index 393→**399**（391 域 + system_status + 7 元），MCP 可达 392→**398**；`03_tools_contract` 603/603 通过（枚举 329、冒烟 273+56 跳过，基线 4 条 warnings 不变）；`tests/config/03_tools_contract.json` 描述计数同步 384→391、324→329；`register_all_test.cpp` 基线补精确断言（399/336/63）。
- **字段增量（只增不改旧字段，缺省兼容）**：`save_editor_scene[_as]` 成功回执 `memory_nodes/disk_nodes/nodes_match`（失配附 `missing_paths`+`warning`，可选 `include_paths/include_hash`）；`save_editor_scene_as` 补 `path`；`create_script` 成功态补 `disk_bytes/content_hash/content_hash_algo(fnv1a64)`；`get_editor_file_system_tree` 加 `depth/prefix/filter`；`property_get` 加批量 `properties[1..32]`（互斥、部分成功+`missing`）；`get_debugger_errors` 加 `group:true` 轻聚类；`execute_game_script` 加 `assert` 沙箱表达式；数字错误码统一 `error_code/error_name/hint`（49 项映射，保留数字）；`code_execute` 注册描述消自矛盾，总纲/gotchas/分册 Skill 补根布局配方、重挂警告、组装顺序、帧精确四配方。
- **绑定与传输**：`call_method` 失败路径附加 `diagnosis{method,node_class,expected_hash,has_method,has_script_method,native_bind_found,suspected_cause}+hint`（报告 F3 的 MotionMode 参数归因不成立，`is_on_wall` 五版零参稳定，改为哈希自检+脚本探针回退）；游戏侧 `send_response` 改 `String::utf8` 构造（P2-1 定界：`String(const char*)` 为 latin1 构造，CJK 元数据乱码根因；`debugger_access.cpp` 同病两处与 `VariantJson` 内约 8 处列为遗留待排期）。
- **验证**：`cmake --preset debug` 配置通过；构建一次失败经两次退回修复（`op_get_errors_grouped` 匿名命名空间链接不可见→移至 `game_bridge` 命名空间；`game_bridge_verify.cpp` 缺头包含/`Viewport` 强转/变量重名 11 处）后通过；L1 243/243（含新基线与 `skill_gen_test`）；25 份 L2 在干净 Example 态全绿（17 批量 + 7 单跑 + 03），新工具探针验收 12 项中 11 项一次通过（`create_script` 回执键位于 `result` 信封内，探针初版路径误写，纠正后通过）。
- **L2 环境残余风险（与本批次无关，已隔离验证）**：E2E 残留的 `Example/scenes/main.tscn` + `run/main_scene` 使无头编辑器在首次文件扫描完成后约 0.5–0.8s 自开主场景（只读探针复现：空查询 w1–w5 后 w6 起根变为 MainMenu；移开 `scenes/` 后翻转消失），与测试 `before_all` 干净场景竞态致 7 份 L2（01/02/11/14/15/18/28）在污染态失败，干净态 7 份全过； failing handler 所在文件（`scene_ops`/`tilemap`/`spriteframes` 等）本批次零改动。`Example/project.godot` 的 34 行 E2E 增量（main_scene/960×540/输入动作）保持原样未动，无 `default_bus_layout.tres` 新增。
- **新增构建接线**：`scene_verify.cpp`、`script_patch_ops.cpp`、`game_bridge_verify.cpp`、`scene_spec_ops.cpp` 入 `add_library`；`scene_verify` 同步 L1 业务源；5 个新工具 schema 条目补齐（`verify_scene_saved` 已有）。
- **时间真实性**：本节时间为真实时间 2026-09-18 19:30 +08:00；历史日志零改写。
