# 2026-09-08 维护日志

## 2026-09-08-00

- **一键生成 Agent Skills 功能**：新增 `src/util/skill_gen.cpp/hpp`（命名空间 `godot_autopilot::skill_gen`，纯函数生成器，无 Godot API）与 7 个 `skill_content_*.cpp` 内容编译单元，聚合 19 册英文 Agent Skills（符合 agentskills.io 规范：name 小写连字符与目录一致、description ≤1024），其中 physics-navigation、rendering-text、resources-files、tool-map 4 册另带 `references/` 子文件（CATEGORY_INDEX/TOOL_REFERENCE 全工具表）。
- **UI 按钮**：`McpConfigDock` 面板 Generate 按钮旁新增 "Generate Skills" 按钮，槽函数 `_on_generate_skills()` 建目录后写入 `<res://>/.agents/skills/<name>/SKILL.md`；`render_skill_md()` 渲染 YAML frontmatter（version 注入 `GDA_VERSION`）；覆盖写仅限自有 19 册命名空间，不触碰其他文件。
- **构建与测试**：根 `CMakeLists.txt` add_library 与 `tests/CMakeLists.txt` GDA_UNIT_BUSINESS_SOURCES 各追加 8 个 `src/util/skill_*.cpp`；新增 `tests/unit/skill_gen_test.cpp`（SkillGenTest 7 用例：19 册完整性、name 规范与目录一致、description 上限、文件布局无 PLACEHOLDER、frontmatter 渲染、工具名回验、references 声明）；ctest L1 103/103 全绿。
- **知识库同步**：`modules/support.md` 新增 skill_gen 小节，审计日期与 frontmatter timestamp 同步至 2026-09-08；`changelog/log.md` 摘要追加当日条目。

## 2026-09-08-01

- **知识库审计与修复**：三代理配对审计——support.md 17 项核对 2 处精确性修正；tests.md 7 处数字修正 + 新增 skill_gen 测试小节；AGENTS.md 103 gtest/110 注册点同步；index/overview/build 修复过时数字与清单（L1 96→103、ctest 103→110、CMakeLists 164→172 行、.cpp 77→85、src/util 5→13，overview 的 src/util 清单补全 skill_gen 与 project_path 并修正 header-only 指代）；security_contract 第 6 节补 UI "Generate Skills" 按钮写盘面（固定写 `<res://>/.agents/skills/`、仅自有 19 册命名空间、不经 MCP 暴露）；roadmap 已交付清单补 skill_gen 条目；support.md Util 节补 project_path.hpp 小节（路径规范化/穿越拒绝，与 security_contract 呼应）；受影响页面 frontmatter timestamp 与审计日期同步至真实时间。

## 2026-09-08-03

- **skill 内容外置化重构（迁移零改写 + 构建期嵌入）**：19 册正文从 7 个 `skill_content_*.cpp`（已删除）迁出为真实 Markdown——新增 `src/util/skill_templates/`（23 个 .md + registry.json；registry 含 19 条 name/description/files 映射，聚合顺序固化于此）与 `tools/embed_skills.py`（纯 stdlib；构建期 5 项校验：19 条/name 规则唯一/description ≤1024/files 结构与 source 存在/孤儿 .md 与重复引用；定界符 `gda_s` 碰撞自动避让；失败非零退出）。
- **构建接线**：新增 `cmake/skill_gen.cmake`——`find_program(GDA_PYTHON_EXECUTABLE NAMES py python python3 REQUIRED)`（py launcher 优先，规避 WindowsApps 存根）+ configure 期 `--version` 自检（失败 FATAL_ERROR）+ `add_custom_command` 生成 `build/generated/skill_content_embedded.h`（约 194KB，gitignore 覆盖）+ `add_custom_target(gda_skill_embed_header)`；新增薄胶水 `src/util/skill_content_generated.cpp`（`embedded::all()` → `SkillSpec`），`skill_gen.hpp` 7 个 make_* 声明收敛为 1 个 `make_embedded_skills()`，`all_skills()` 改为其转发（顺序由 registry.json 固化）；根与 tests 两处 CMakeLists 源列表 8→2 个 skill 源并各加 `add_dependencies(... gda_skill_embed_header)`。内容变更不再触碰 C++。
- **验证**：编译通过；ctest L1 103/103 全绿；`tests/unit/skill_gen_test.cpp` 零修改——19 册内容逐字节不变（M1 迁移自校验 ALL OK）。
- **知识库同步**：support.md skill_gen 小节改写内容承载描述（模板目录 + 构建期嵌入 + 构建期校验 + py launcher 自检）；build.md 数字同步（CMakeLists 172→169 行、add_library .cpp 85→79、cmake 模块 6→7、include 行号 49-54→52-58，补 skill_templates 不进 add_library）；tests.md GDA_UNIT_BUSINESS_SOURCES 显式 23→17 + skill_gen 测试小节补外置化说明；AGENTS.md skill_gen 行与 add_library 行号（64→65）同步。

## 2026-09-08-04

- **文档审计修正（skill 外置化代码-文档配对复核，零代码变更）**：overview.md 修正 src/util 目录树注释（已删除的 7 个 `skill_content_*` → `skill_gen`、`skill_content_generated` 构建期嵌入薄胶水；补内容目录 `skill_templates/` 23 个 .md + registry.json）；tests.md 4 处 CMake 行号按实测校正（`GDA_ENABLE_TESTS` 139→164、`--report-dir` 112→116、L2 ctest 注册块 105-114 与 108-114→112-119）；build.md WHOLEARCHIVE 行号 133→158；overview/build/tests/support 与根 AGENTS.md 共 4 处生成头路径 `build/generated/` 精确为 `build/<preset>/generated/`；overview/build/tests/support frontmatter timestamp 同步。数值复核（全部一致）：CMakeLists 169 行 / add_library 79 个 .cpp / cmake 7 模块 / include :52-58 / add_dependencies :146 / registry.json 19 条 / skill_templates 23 个 .md / 生成头实测 194,820 字节 / skill_gen_test 7 用例名逐字一致。
