# 2026-08-16

## 2026-08-16-16

- **mcp-cpp-sdk 升级 0.2.2 → 0.3.1**（破坏性重构升级）：0.3.x 三大重构——移除 libhv（自研 socket 网络栈）、移除 simdjson（自研 JSON 解析器）、SDK 测试框架自研化（项目不受影响）。影响面分析：`mcp::JsonValue` 头零差异、`McpServer`/`McpClient`/`StreamableHttpServerTransport`/`ServerOptions` 签名兼容、`InputRequiredConfig` 3 字段聚合初始化仍合法；仅 3 处必须修改——`FetchDependencies.cmake` GIT_TAG→0.3.1、`server_context.cpp` 删除 `hv/hlog.h` + `hlog_disable()`（SDK 日志默认 Off）、`tests/CMakeLists.txt` 移除 `hv_static` 链接
- **行为变化记录**：HttpServer 新增 DNS rebinding 保护（Host 非 localhost/127.0.0.1/::1 返回 403，局域网 IP 访问会被拒）；`stateless` 默认值 false→true（项目已显式设置无影响）；新增 SEP-2549 响应缓存/认证挑战重试等未使用功能
- **全量验证**：`uv run build.py` 构建成功（0.3.1 编译通过、插件 DLL 部署）；ctest 66/66 通过（L1 61 gtest + L2 5 headless 编辑器真实 MCP HTTP 闭环）
- **文档全量同步**：AGENTS.md（依赖列表/线程模型 3 处）、README.md / README_zh.md（架构图/技术栈 3 处）、docs/wiki/（overview/build/conventions/modules/core/modules/entry_runtime）、Example/docs/architecture.md —— 18 处 libhv/simdjson 引用更新为 SDK 自研网络栈/解析器描述

## 2026-08-16-17

- **知识库更新规则完善**：AGENTS.md「项目知识库」新增「审计日期同步」（带页头日期行的页面修改后必须更新为当天并标注原因，无日期头页面不新增）与「日志联动」（修改页面/代码须在当天 changelog 追加记录）两条规则；docs/wiki/index.md 维护入口同步补充审计日期步骤
