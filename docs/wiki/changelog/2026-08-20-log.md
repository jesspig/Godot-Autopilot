# 2026-08-20 维护日志

## 2026-08-20-23

**ToolBase 工具统一标准化落地（ToolRegistry 单一来源）+ 验证**：

- **新增基础设施**：`src/tools/tool_base.hpp`（`ToolBase` 根接口 + `ToolMeta` + `ArgReader` + `make_ok/make_error` + 角色接口 `IExportGuard/IAsync/ISideEffect` 及 `blocks_export/tool_is_async/side_effect_of` 助手）、`tool_registry.hpp`（`ToolRegistry`：add/add_meta/find/all_any/categories/to_tool_info_all，header-only）、`fn_tool.hpp`（`FnTool` 适配器，包既有 handler 为 ToolBase）。
- **register_all 重构为 registry 单一来源**（`register_all.cpp`）：一次 include `tool_defs.def`（336 域）+ system_status + 7 元工具(`add_meta`)全入文件级静态 `ToolRegistry`，再从 `all_any()` 派生 catalog(344)/BM25 index，从 `all()`/`all_meta()` 派生 `g_handlers`/`g_meta_handlers`，循环 `RegisterTool` 7 元工具；原双 include/`populate_default_tools`/meta 快照/Auto 补录链路废弃。
- **生命周期修复**：`g_active_registry` 为文件级 `static unique_ptr<ToolRegistry>`（程序存活期）避免函数局部对象悬垂；RegisterTool/meta handler 只捕获工具名字符串。
- **测试**：新增 L1 `tests/unit/tool_registry_test.cpp`（4 用例）+ `register_all_test.cpp` 增 `RegistryIsSingleSourceOfTools`（meta_size==7、all_any.size()==catalog.size()、双向对齐）；L1 **77/77** + L2 **6/6**（含 03_tools_contract 336 工具全遍历）通过。
- **说明**：元工具 search/list/get_detail 不再包 `queue.submit`，但仅访问线程安全 catalog/Bm25Index，行为等价；"逐域真类迁移"（M1–M8 把 FnTool 换手写子类）与 `side_effects()` 驱动遍历排除未落地。
- **文档**：本行日志 + `tool_base_design.md` 状态改"核心已落地"并更新 timestamp；`tools_registry.md` 重构注册管线/三层结构/计数（344=336+system_status+7 元，遍历步数与 def 分类细分标注待重算）；`index.md`/`tests.md` 计数与 timestamp 同步（L1 77、L2 6、unit 10 文件）。

## 2026-08-20-22

**ToolBase 工具统一标准化设计定稿（未实现，仅立项）**：

- 定稿设计取向：**接口 + 组合**（弃早期"ToolBase + 域基类继承"）。`ToolBase` 作纯虚根接口，横切关注点拆小角色接口按需实现（`IExportGuard`/`IAsync`/`ISideEffect`/`ISchemaField`），域共享逻辑改为可注入助手对象（如 `SceneEditorAccess`）经构造函数组合。
- 明确"自动发现 vs 自动注册"边界：C++ 无反射，不做运行时扫描；采用类内静态 `ToolMeta` 元信息自动推导 + `GD_TOOL_LIST` 类名清单，新增工具收为"新增类 + 清单 1 行 + CMake"。
- 方案要点：`ToolRegistry` 取代 `g_handlers`/`g_meta_handlers`/`ToolCatalog` 三容器；`tool_defs.def` 双 include 演化为纯类名清单；元工具注入依赖走 `add_meta` 最后注册消除递归；`side_effects()` 替代遍历硬编码排除清单；行为对等红线（输出键/错误语义/主线程契约）由 L1/L2 守住。
- **知识库**：新增设计提案页 `docs/wiki/tool_base_design.md`（frontmatter 标记"设计提案"、`[!todo]` 待实施），`index.md` 页面索引补录一行。**未动任何代码**，当前工作树注册管线不变。

## 2026-08-20-16

**依据试用反馈 5 类问题落地 rename 事务化 + 覆盖度补齐（4 个新工具）**：

- **P0 修复 `rename_resource_file` 双断链**（`resource_ops.cpp` `handle_rename`）：① 改名先搬运伴生 `.uid` 文件保 uid 不重生成（P0-2）；② rename 前扫描 res:// 全部 `.tscn/.tres` 收集依赖，rename 后回写 `path="旧"` → `path="新"`（P0-1）；③ 返回结构化影响报告 `{result, updated_files:[{file,changes}], stale_references:[{file,reason}], uid_preserved}`（P2-1）
- **`script_class` 不再经 `ResourceLoader.load` 确认**（曾因重入编辑器文件系统存在崩溃风险），命中即列 `stale_references`；写回前关闭读句柄、写后 flush/close 并回读验证，验证不过入 stale 而非乐观上报
- **P1-1 修复 `property_set` NodePath 不写 `node_paths`**（`property_ops.cpp`）：Node 类型导出属性遇 NodePath 值自动解析为节点引用赋值（场景根 `get_node`），失败带用法说明，成功标 `converted_node_path`
- **新增 4 个域工具**（总数 339→343、领域 332→336、ToolCatalog 347）：`get_resource_references`（引用反查，Resources）、`read_file`/`find_in_files`（text_ops，OS 类）、`build_csharp_assembly`（`dotnet build` 子进程，Editor 类，含能力边界 `note`）；各自四步注册（def + schema + hpp 声明，复用既有 .cpp 无 CMake 改动）
- **工程要点**：`collect_text_file_paths`/`find_in_files_recursive` 用 `join_path`/`join_search_path` 拼接，避免 `res:///` 三重斜杠（否则 FileAccess WRITE 提交失败残留孤儿 `.tmp`）
- **测试**：新增 L2 用例 `tests/config/05_rename_references.json`（12 步，端到端验证 uid 保活 + 回写落盘 + 反查 + find_in_files）；`build_csharp_assembly` 入遍历副作用排除清单（34→35）；全量 ctest **78/78 通过**
- **文档**：overview / tests / conventions / tools_ops_a / tools_ops_b 同步（计数 + 能力边界 + C# 只读提示）；`Example/project.godot` 引擎副作用已还原

> 附带认知：GitHub 网络检索确认 GDExtension(C++) 无公共 API 触发编辑器内 C# 程序集热重载（能力在引擎 `modules/mono` 内部），`build_csharp_assembly` 仅能触发编译，已如实写入工具 `note` 与 conventions 能力边界。