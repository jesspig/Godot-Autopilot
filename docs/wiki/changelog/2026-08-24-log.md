# 2026-08-24 维护日志

## 2026-08-24-05

- **竞品对齐批次 wiki 沉淀**（功能分支 `feature/engine-aware-fs-and-tool-expansion`，代码侧 P0/P1/P2 已交付、83 个 ctest 全绿）：
  - 新建 `plans/roadmap.md`——竞品全景一行定位速记表（godot-ai / Godot MCP Pro / GodotPilot / Coding-Solo/godot-mcp / IvanMurzak/Godot-MCP）+ 本轮 P0 文件操作收编、P1 AI 易用性、P2 覆盖扩展交付清单 + 遗留 P3（客户端一键配置扩展、AssetLib 上架）
  - `modules/tools_registry.md`——类别分布表重算为 27 类/363 域工具（新增 Analysis 3/Animation 10/Theme 8/Testing 2；Resources 24/Scene 14/Game 9/Debugger 5），三层结构 371 口径，副作用排除清单重分组 42 = 磁盘 20 + 用户可见 16 + 进程 6，遍历口径改"363 枚举/42 排除/321 空参"，契约缺口表修正 reimport_resource_files 行（schema path+files 双字段已对齐 handler，字段名不一致缺口消除；空参 count=0 静默成功仍在）
  - 死引用清理：`modules/tools_ops_b.md` 与 `modules/support.md` 中被删工具 get_debugger_stack_dump/get_debugger_monitors 的引用改为指引 execute_game_script/get_game_log_entries/get_game_status/debugger 资源 URI；debugger_ops 7→5 工具、runtime_game_ops 7→9
  - `modules/core.md`——新增 error_watermark（doorbell 三函数与接线点）与 editor_readiness（is_import_in_progress/busy_error）两小节
  - `modules/entry_runtime.md`——协议 op 表补 input_sequence/ui_elements；game_bridge_input.cpp 的 GameBridgeFrameSequence 帧调度（at_frame 相对偏移、256 步上限、33ms/帧超时推导、钳制 30000）与 ui_elements 响应形状（{path,type,visible,text?,global_rect}、max_elements 默认 100 上限 1000）
  - 根 `AGENTS.md` 数字同步：363 域/30 个 tools.hpp、370 总计、371 条目、42 副作用、77 gtest、ctest 83=77+6、27 类别、遍历 363 枚举/321 空参；删除已修复的「code_execute.timeout_ms 无上限钳制」知识局限条目
  - `index.md`——数值速查全量刷新，页面索引加 roadmap 入口、ops_a 计数修正（17 模块/201 工具）、tool_base_design 行 363/30
- **验证**：全部数字经源码实测核对——`*_tools.hpp` 枚举 363/42（PowerShell 正则逐文件统计）、类别分布按 Category 字段聚合求和=363、gtest 以 `TEST(/TEST_F(` 计数=77、reimport schema/handler 双字段对齐读码确认、BM25 CJK bigram 与 GameBridgeFrameSequence 读码确认

## 关键数字对照（本轮前后）

| 口径 | 前 | 后 |
|---|---|---|
| 领域工具 | 336 | 363 |
| 元工具 / MCP 可达总数 | 7 / 343 | 7 / 370 |
| ToolCatalog/BM25 条目 | 344 | 371 |
| 类别数 | 23 | 27 |
| 副作用工具 | 35 | 42 |
| 遍历枚举/空参 | 336/301 | 363/321 |
| L1 gtest / ctest 测试点 | 71 / 77 | 77 / 83 |

## 2026-08-24-16

- **修复 move/rename 引用未更新 bug**（用户实测 move_resource_file 后引用失效且运行时报错；根因对照引擎 dock 管线定位）：
  - 事务管线补"打开场景协调"闭环：受影响打开场景先 `save_all_scenes()` 落盘 → 磁盘改写 → `reload_scene_from_path()` 重载（活动场景排最后保焦点）——此前磁盘改写会被编辑器 autosave 静默写回旧路径，为运行时报错主因
  - `.import` sidecar 随迁（此前仅迁 `.uid`），失败时响应附 `import_sidecar_warning`；导入型资产移动不再丢 uid 与导入配置
  - 每个被改写的引用方文件即时 `update_file` 刷新 EFS deps 图（消除二次移动漏判 owner 的窗口）
  - uid 探测修正：十进制 token（恒 false 死代码）改为 `ResourceUID::id_to_text` 文本形式（remove 预检同步修正），删除永不触发的 uid 改写分支
  - 二进制资源（.scn/.res/.csv/.translation）引用无法就地改写，显式降级为顶层 `unsupported_binary_references` 报告并引导走编辑器
  - `handle_move` 的 saved/reloaded/import warning 三字段从 moved 元素内提升至顶层聚合
  - `move_os_file_to_trash` 支持 res://与 user:// 自动 globalize_path（原样传 OS API 对项目路径静默失败）
  - 新增 L2 用例 `06_move_references.json`：14 步含 cleanup 自回收（close 场景 + trash 临时目录），阻断编辑器布局恢复导致的跨轮污染；`reloaded_scenes not_empty` 断言强制覆盖打开场景分支
- **验证**：84/84 ctest 全绿（新增 1 个 L2 测试点）；连续两轮 06 用例通过确认污染链已断、临时目录真实回收
