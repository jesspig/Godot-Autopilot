# 2026-08-13

## 2026-08-13-13

- **新增 `reload_game_scripts` 热重载工具**（Game 类）：向全部 active+ready 调试会话广播引擎原生 `reload_scripts`（指定 paths）/`reload_all_scripts`（空路径分支，handler 层强制 paths 非空）消息，运行中脚本软重载不重启游戏；返回 `sent_sessions` + note 提示经 `get_game_log_entries` 验证。领域工具 331→332、总数 338→339、ToolCatalog 342→343、Game 类 6→7（`tool_defs.def` 实测 332 条 TOOL_ENTRY）
- **create_script 写盘读回校验**：保存后从磁盘读回与 source_code 比对，返回 `verified`/`readback` 字段；锁文件写入失败不再静默成功，返回 `write_issue` + `warning`（并提示勿并行写同一文件）
- **execute_game_script 错误回传**：游戏侧脚本错误（SCRIPT ERROR）经响应回传，`error_details`/`runtime_error` 合并进 result（或附加到错误消息），不再静默超时；超时文案增加 `get_game_log_entries` 查日志指引
- **call_method 支持 await 方法**：检测 `GDScriptFunctionState` 返回值，经 `GameBridgeEvalAwaiter` 等待至 timeout_ms 上限并返回最终结果；超时返回错误，不再返回状态对象
- **输入瞬态文档补充**：`queue_game_input`/`wait_game_input` 描述与 schema 注明瞬态状态（is_action_just_pressed）仅物理帧可见，`_process`（渲染帧）查询不到
- **文档全量同步**：AGENTS.md（架构/测试/知识局限 3 处数字）、README.md / README_zh.md（~338→~339、Game 6→7）、docs/wiki/（index/overview/tests/tools_registry/tools_ops_a/tools_ops_b/build）；tests.md 新增「L2 环境漂移」说明——Example/project.godot 含 run/main_scene 且 .godot/editor 缓存记录最近场景时，headless 编辑器异步恢复主场景覆盖 before_all 的 Root 场景致 01_scene/02_property 失败（与代码无关，运行 L2 前清理）；遍历步数静态推算 408→410（298 空参 + 112 冒烟）

## 2026-08-13-15

- **测试开关持久化**：`GDA_ENABLE_TESTS` 固化进 `CMakePresets.json` 的 debug/release 预设（cacheVariables 默认 ON）——此前该开关仅存在于 `build/` 的 CMake 缓存，重新配置（`uv run build.py` / `cmake --preset debug`）或清理 `build/` 后即丢失，导致 `ctest` 报 "No tests were found"；现清理重建后自动恢复测试目标。裸 `cmake`（不带 preset）仍默认 OFF。AGENTS.md / docs/wiki/tests.md / tests/README.md 的「默认 OFF + 手动传参」描述同步更新
- **全量测试复验**：debug 与 release 预设各 66/66 通过（L1 61 gtest + L2 5 headless 编辑器闭环，`ctest --preset debug|release`），测试注册、构建、运行全链路在当前工作树状态验证；Example/project.godot 当前为仓库基线版本（无 run/main_scene，L2 兼容态；含主场景的版本备份于 `%TEMP%\opencode\project.godot.bak`）
