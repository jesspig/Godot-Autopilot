# 2026-09-10 维护日志

## 2026-09-10-01

- **skill 体系 19→7 册重构**：19 册合并为 1 册插件总纲 `godot-autopilot`（吸收原 usage/direct-http/tool-map/tips-gotchas）+ 6 册引擎指南 `godot-autopilot-{scene-system,resources,scripting,runtime,servers,content}`（scene-system 吸收原 scene-building + properties-signals + inspection 编辑侧、runtime 吸收原 running-games + debugging + inspection 运行侧、scripting 就地重写保留、resources 吸收原 resources-files、servers 吸收原 physics-navigation + rendering-text、content 吸收原 tilemap + animation + audio + ui-theming + spriteframes），每册带 references/ 子文档渐进披露（合计 20 个）；`src/util/skill_templates/` 现 28 个文件（27 个 .md + registry.json，registry 7 条定稿），22 个旧模板删除；`tools/embed_skills.py` 的 `SKILL_COUNT` 19→7。
- **Godot 源码研究发现织入**：84 条 Godot 4.8.0-dev 源码研究发现全部织入引擎六册（撤销三历史、RID 不级联、暂停矩阵、just_pressed 双计数器、调试三道闸门、uid 优先于 path、导航双缓冲、set_cell 静默清格等），行内以 4.7+/4.8 简注标注适用版本。
- **dock 按钮动态化**：McpConfigDock `has_existing_skills()` 检测 `.agents/skills/` 下 `godot-autopilot-` 前缀目录，按钮文本动态 "Generate Skills"/"Update Skills"；Update 点击先 `remove_dir_recursive`（后序遍历 + `DirAccess::remove`）递归清理全部前缀匹配目录再重新生成（旧 19 册在用户项目中自动退役）；成功文案动态计数 "Generated/Updated N skills ... M reference files"。
- **测试**：`tests/unit/skill_gen_test.cpp` 7 册期望清单、用例改名 `AllSevenSkillsPresent`/`EverySkillDeclaresReferences`（每册 ≥1 references 断言）、白名单扩至 176 项；ctest L1 103/103 全绿。
- **知识库同步**：modules/support.md skill_gen 小节与 McpConfigDock 小节 7 册化、tests.md skill_gen 测试小节 7 册化（白名单 176）、overview.md 目录树注释与 McpConfigDock 描述、根 AGENTS.md skill_gen 行；受影响页面 frontmatter timestamp 同步至真实时间。

## 2026-09-10-02

- **版本号 0.2.2 → 0.2.3**：根 `VERSION` 单一来源变更（CRLF 格式保留，xxd 验证）；`AGENTS.md` 构建节与 `build.md` validate 示例同步（`v0.2.3` ↔ `0.2.3`）；重新 configure 后 `GDA_VERSION "0.2.3"` 注入验证通过，ctest L1 103/103 全绿（frontmatter version 由测试动态比对自动适配）。
