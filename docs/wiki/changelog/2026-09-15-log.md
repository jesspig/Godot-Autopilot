# 2026-09-15 维护日志

## 2026-09-15-07

- **Computer Use grounding 增强（域工具 366→379）**：新增 13 个工具——编辑器 UI 语义自动化（`get_editor_ui_elements`、`hit_test_editor_point`、`click_editor_element`、`type_editor_element_text`、`run_editor_shortcut`）、原始坐标合成输入（`click_input_mouse`、`scroll_input_mouse`、`drag_input_mouse`、`type_input_text`）、坐标映射（`get_editor_viewport_geometry`、`get_scene_node_screen_rect`、`get_display_window_rect`）与游戏侧 `click_game_ui_element`（节点 path 匹配 + 坐标注入在游戏进程内完成，单次协议往返避免主线程死锁）；`capture_editor_viewport` 新增 `region`/`max_dimension`/`space`/`annotate`/`diff_against_last`，游戏侧 `op_capture` 支持 region/max_dimension/annotate，`queue_game_input`/`sequence_game_inputs` 新增 wheel（`direction`/`amount` 或 button_index 4-7）与单步 `mouse_motion`。
- **计数与分类**：域工具 379 = 322 非 SIDE + 57 SIDE（writes_file 15 / writes_config 6 / shows_alert 4 / modifies_window 12→19 / process 6 / game_runtime 5→6 / code_execute 1）；MCP 可达 386、catalog/index 387；分类 Editor 23→29、Input 11→15、Display 24→25、Scene 6→7、Game 9→10（仍 27 类）。
- **截图 diff 语义**：与上次编辑器截图比较；尺寸不同时按公共左上区域比较并返回 `size_mismatch=true`/`current_size`；不可比较时 `comparable=false` + `reason` 仅 `unsupported_format`/`no_baseline`/`baseline_too_large`。
- **安全**：新增 SIDE 工具全部标记——编辑器侧 7 个 `ModifiesWindow`、游戏侧 1 个 `GameRuntime`；未新增授权能力门。
- **测试**：L1 140→172（unit 文件 15→16，新增 `editor_coords_test` 32 项），L2 9→11（新增 `09_editor_ui.json`、`10_editor_input.json`），ctest 149→183；`03_tools_contract` 遍历 322 个非 SIDE 工具、0 失败，warnings 实测 4 条（`start_input_gamepad_vibration`、`stop_input_gamepad_vibration`、`get_resource_extensions`、`reimport_resource_files`；`create_scene_node` 是否告警取决于运行期是否有场景）。
- **验收**：GUI 模式验收用例（build 目录临时产物）10/10 通过；已知残留——游戏侧 wheel/click/annotate 仅编译与代码级验证（L3 双进程 E2E 未实现），编辑器元素点击/输入的自动化端到端受 L2 执行器不支持跨步骤变量引用所限未自动化。
- **技能与知识库**：skill 仍 8 册，4 册模板更新（`autopilot.md`、`autopilot--tool-catalog.md`、`autopilot--development-workflow.md`、`runtime--input-injection.md`，工具目录 379 与编辑器 UI 自动化闭环）；知识库同步 `overview`、`index`、`modules/tools_registry`、`modules/tools_ops_a`、`modules/tools_ops_b`、`tests`、`security_contract`、`modules/support`、`plans/roadmap`、`build`、`conventions`、`tool_base_design`。
