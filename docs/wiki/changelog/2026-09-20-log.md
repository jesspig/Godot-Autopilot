# 2026-09-20 维护日志

## 2026/09/20 17:20

- **代码-文档一致性审计（AGENTS.md 自动维护触发）**：工作树干净（`release/0.2.6`，`git status` 无改动），以当前文件逐项重核，未引入新功能。
- **冲突消解（源码 1 处）**：`src/tools/register_all.cpp:406` 的 `call_tool` 描述仍为旧口径"386 非元（385 域 + `system_status`）"，与 30 域 `*_tools.hpp` 的 `make_spec_tool` 静态求和 391 实测不符；`overview.md` 的 09-18"已随源码修正"注记失实（源码并未改动，系审计记录错误）。本轮把描述修正为"392 非元（391 域 + `system_status`）"（纯文本，无行为影响；L1 仅断言描述非空，无精确断言），并更正 overview 注记。
- **计数刷新（wiki 6 页 + AGENTS.md）**：ctest 口径长期漏计 09-19 注释清理批新增的 `comment_guard`——`ctest -E "^gda_runner_"` 270→271（269 gtest + 2 守卫，09-19 实测 271/271 全绿为证），全量 ctest 注册点 298→299（271 + 28 L2）；`tests.md` 新增"守卫"小节补 `comment_guard` 文档（此前全库零记载）；`index.md`（255/26 文件/27 L2/283→269/29 文件/28/299）、`overview.md` 目录树（255/27→269/28）、`build.md`（add_library 分目录 tools 50→48 / util 7→8 / runtime 4，总数 83 不变）、`plans/roadmap.md`（283→299）同步。
- **行号重核（core/entry_runtime）**：`debugger_ops.cpp:683`→`:763`（`get_plugin_log` 的 `since_index` 处 `query_from`）、`main.cpp:258-325`→`:291-357`（`GDExtensionEntryPoint`）、`main.cpp:46-54`→`:47-55` 与 `:46`→`:47`（`gda_cmdline_mode`）、`game_bridge_eval.cpp:550/:569`→`:534/:553`（诊断函数定义）；文件行数 `main.cpp` 326→358、`gda_protocol.hpp` 53→57、`game_bridge.hpp` 79→99、`game_bridge_eval.cpp` 799→851。
- **复核一致（零改动）**：391 域求和（A 196 + B 195）与 27 类分布表自洽（Input 23 = input 15 + input_map 8 等）；flags（kCaptureImage 9 / kSceneTarget 25 / kUndoable 10）、schema 基线（399 = 336 + 63，`register_all_test.cpp:142-144`）、遍历（392 枚举 − 62 排除 = 330，上限 660 步）、20 客户端、`SKILL_COUNT = 8`、版本单一来源（`VERSION` 0.2.5 → `CMakeLists.txt:33`/`build.py:34`）、`release.yml` 双格式触发、`Example/project.godot` 39 行/173 PNG/11 文档均与源码一致；`read_file` 三工具归 `os_tools` 注记属实。
- **遗留**：AGENTS.md 的"84 条研究发现"在库内无机器可核来源（skill 模板仅 10 处 `4.8` 行内注），系历史讨论记录口径，未改动；`log.md` 仍为最近 7 天（09-14…09-20），无需裁剪；`## 知识库` 章节已承担"项目知识库"维护规则职责，未新增重复章节。
- **时间真实性**：本节时间为真实时间 2026-09-20 17:20 +08:00；历史日志零改写。

## 2026/09/20 17:26

- **0.2.6 升版（对齐发布 tag）**：CI `release.yml` validate 拦截 tag `0.2.6`（`VERSION` 仍为 `0.2.5`，run 35502180800，10s 失败属守卫按设计工作）；根 `VERSION` 0.2.5→0.2.6（版本号单一来源，`GDA_VERSION`/打包名经 `CMakeLists.txt:33`/`build.py:34` 派生，CI 构建期重新 configure 生效，本地未重配）；同步 `AGENTS.md` 构建段与 `build.md` validate 示例（`v0.2.6` ↔ `0.2.6`）+ 审计行 + frontmatter `timestamp`；17:20 节审计记录保持原样（当时实测确为 0.2.5）。
- **tag 重打**：本地 `0.2.6` 移至本提交后强制推送，`on.push.tags` 双格式重新触发 release。
- **时间真实性**：本节时间为真实时间 2026-09-20 17:26 +08:00；历史日志零改写。

## 2026/09/20 17:08

- **Release 触发修复（未提交）**：根因——`.github/workflows/release.yml` 的 `on.push.tags` 仅 `v*`，而 7 个已发布 tag 全是无 `v` 格式（`0.1.0`…`0.2.5`，`VERSION` 亦为 `0.2.5`），workflow 一次都没触发过，故零 release；`validate` job 同样按 `v${version}` 硬校验，与既有 tag 不兼容。
- **改动**（仅 `release.yml`）：`on.push.tags` 改为双格式 `v*.*.*` / `[0-9]*.*.*`；新增 `workflow_dispatch`（`tag` 必填输入，用于手动补发/重跑）；`validate` 改为剥离 `v` 前缀后再与 `VERSION` 比对（两种格式都过）；`validate`/`build`/`package` 三处 `checkout` 统一 `ref: inputs.tag || github.ref`（手动触发时 checkout 到指定 tag 而非默认分支）；`softprops/action-gh-release` 显式传 `tag_name: inputs.tag || github.ref_name`；`concurrency` 组改为 `release-inputs.tag || github.ref`。
- **验证**：YAML 解析通过（`on.push.tags = ['v*.*.*', '[0-9]*.*.*']`、`workflow_dispatch.inputs = ['tag']`、`tag_name` 占位符就位）；版本比对逻辑本地模拟——`0.2.5`/`v0.2.5` 通过、`0.2.4`/`v0.2.4` 拒绝（符合预期）；`git diff` 确认仅 1 文件 +20/−4。
- **wiki 同步**：`build.md`（触发描述 + validate 口径 + 审计行 + frontmatter `timestamp`）、`overview.md`（一致性核查行 + 事实来源下补 09-20 行 + frontmatter `timestamp`）。
- **决策留痕**：tag 命名采用双格式兼容；历史 7 个 tag 暂不补发（GitHub 上有 tag 无 release 属预期）。
- **时间真实性**：本节时间为真实时间 2026-09-20 17:08 +08:00；历史日志零改写。

## 2026/09/20 21:50

- **可重放监控 + 双目录持久化批次（分支 `feature/observable-traces`，未提交）**：新增 `src/core/log_persist.{hpp,cpp}`（日志/trace 双目录落盘与修剪）、`src/core/trace_recorder.{hpp,cpp}`（结构化事件环形缓冲 + 脱敏/哈希/base64/JSON 行纯函数）、`src/core/sanitize_policy.{hpp,cpp}`（脱敏开关，优先级 env `GODOT_AUTOPILOT_DESENSITIZE` > config `desensitize` > 默认 true）；`LogSystem` 增 `log_detailed`/`LogEntry.detail`（上限 8192 字符，`filter_text` 兼匹配 detail）；`SpecTool::execute` 逐调用记录 `TraceEvent`（授权拒绝单独埋点、图片只记 `image_ref`/`image_hash`/尺寸）、`tool_invoke` 增线程局部 span 栈与 `capture_trace_context`/`ScopedTraceContext` 跨线程上下文、`dispatch` 测 `queue_wait_ms` 并对未捕获异常埋点；`main.cpp` 入口 `sanitize_policy::initialize()` + `LogPersist::init_session()`、`_process` 先 `flush_on_main_thread()` 再 `drain()`、`_exit_tree` 收尾 flush；McpLogDock 增 Detail 切换与 Open Logs 按钮、McpConfigDock 增 Desensitize data 复选框（写 `desensitize` 键、即时生效）。
- **持久化产物与边界**：`user://godot_autopilot/logs/gda-<stamp>.log`（人类可读，含 detail）与 `traces/trace-<stamp>.jsonl`（结构化事件，首行 `session_start`、服务器就绪追加 `server_ready`）+ `traces/images/`（仅脱敏关闭时落盘 PNG）；两目录各保留最近 20 文件 / 50MB（`LogPersist::should_prune`）；内存侧日志 10000 条 / trace 20000 条 / 待落盘队列 20000 条；写失败一次告警后停用本会话持久化。
- **缺陷修复 2 处**：`FileAccess::READ_WRITE` 打开不创建新文件导致首次 flush 失败 → 失败时回退 `WRITE`（`src/core/log_persist.cpp:29-43`）；`src/tools/text_ops.cpp` 的 `write_file` `mode=APPEND` 同类修复（首次追加不再失败）。
- **验证**：L1 `ctest --preset debug -E "^gda_runner_"` 271→287 全过（新增 `tests/unit/trace_recorder_test.cpp` 11 项 + `tests/unit/log_persist_test.cpp` 5 项；gtest 269→285、unit 文件 29→31）；L2 新增 `tests/config/29_trace_persistence.json`（28→29 份）实测 PASS；全量 ctest 注册点 299→316（285 + 2 守卫 + 29）；工具总数 399 不变，新增 3 个 core 编译单元进入 `add_library`（83→86）。
- **wiki 同步（13 页）**：`modules/core.md`（三新模块职责与接口、LogSystem detail、PluginConfig `desensitize`、flush 顺序、`GODOT_AUTOPILOT_DESENSITIZE`）、`modules/entry_runtime.md`（main.cpp 370 行、入口顺序与 `server_ready`、`_process`/`_exit_tree` flush）、`tool_base_design.md`（新增"执行埋点与可重放 trace"小节、tool_invoke trace 扩展）、`modules/tools_registry.md`（dispatch trace 上下文/排队测量/异常埋点）、`tests.md`（L1 285/31 文件、L2 29 份、ctest 316、新增两 L1 文件与 29 号 L2 行、业务源 25 显式）、`security_contract.md`（新增 3.2 脱敏开关、落盘边界、线程纪律与验收清单）、`modules/support.md`（两 dock 新控件）、`conventions.md`（日志落盘与脱敏约定）、`build.md`（add_library 83→86、core 9→12）、`modules/tools_ops_b.md`（write_file APPEND 回退与 log_detailed 增补）、`overview.md`（目录树/UI/计数）、`index.md`（关键数值）、`plans/roadmap.md`（ctest 316）；带 frontmatter 页 `timestamp` 同步真实时间。
- **MCP resource 读取链路同步接入 trace**：`src/resources/resource_handlers.cpp` 与 `src/resources/debugger_resources.cpp` 为全部 resource 读取回调追加 `capture_trace_context()`/`ScopedTraceContext` 传递（`git diff HEAD` 实测 +48/−15）；wiki 按当前工作树 diff 记入 `modules/support.md` 与 `tool_base_design.md`，该路径未经 L1/L2 专门验证。
- **遗留（禁改范围内的不一致）**：根 `AGENTS.md` 与 `tests/README.md` 仍为旧计数（269 gtest / 28 L2 / 271 / 299 口径），本任务禁改 `src/`、`tests/`、`CMakeLists.txt`、`AGENTS.md`、`README`，待主代理统一同步；`tests.md` 与 `index.md` 的数值已按本轮实测更新。
- **时间真实性**：本节时间为真实时间 2026-09-20 21:50 +08:00；历史日志零改写。

## 2026/09/20 21:56

- **依赖升级（分支 `feature/observable-traces`，未提交）**：godot-cpp `10.0.0-rc2`→`10.0.0-stable`、mcp-cpp-sdk `0.3.4`→`0.3.5`；声明处 `cmake/FetchDependencies.cmake:20/29`（GIT_TAG）。
- **实测依据**：`build/debug/_deps/godot-cpp-src` 的 `git describe --tags` = `10.0.0-stable`（commit `507ed9d`）、`build/debug/_deps/mcp-cpp-sdk-src` = `0.3.5`（commit `2304f1b`）；configure 输出 `[mcp] SDK version: 0.3.5`。
- **当前事实同步（4 文件，零历史改写）**：`AGENTS.md:39` 依赖行；`build.md`（依赖表 `:133` + 对照检查 `:166` + 审计行追加句）；`conventions.md:76`；`overview.md`（技术栈表 `:107/108` + 审计行追加句）。三页 frontmatter `timestamp` 同步真实时间。
- **边界**：历史留痕（09-17 的 rc1→rc2 / 0.3.3→0.3.4 记录、`changelog/2026-09-17-log.md`、`log.md` 历史条目）保持原样；未改 `src/`、`tests/`、`cmake/`；未提交。
- **时间真实性**：本节时间为真实时间 2026-09-20 21:56 +08:00；历史日志零改写。

## 2026/09/20 22:12

- **依赖升级范围调整（分支 `feature/observable-traces`，未提交）**：mcp-cpp-sdk `0.3.5` 的升级尝试与 `FromObject` 适配已回滚，SDK 保持 `0.3.4`（0.3.5 被认为可能有潜在问题）；godot-cpp `10.0.0-stable` 保留。声明文件 `cmake/FetchDependencies.cmake` 相对 HEAD 仅余 godot-cpp 一处变更（`:20` GIT_TAG `10.0.0-rc2`→`10.0.0-stable`；`:29` SDK GIT_TAG 仍为 `0.3.4`）；`git diff HEAD` 复核 `src/util/json_godot.hpp`、`src/tools/environment_ops.cpp`、`src/tools/nav_ops.cpp` 的 `FromObject` 相关改动已全部回收，工作树无 SDK 适配残留。
- **实测依据**：重新 configure 输出 `[mcp] SDK version: 0.3.4`；`build/debug/_deps/mcp-cpp-sdk-src` 的 `git describe --tags` = `0.3.4`（checkout `e2ed352`）、`build/debug/_deps/godot-cpp-src` = `10.0.0-stable`（commit `507ed9d`）。
- **文档更正（5 文件）**：21:56 节写入的"mcp-cpp-sdk 0.3.4→0.3.5"及其 configure 实测 0.3.5 系升级尝试的中间状态，已回滚；`AGENTS.md:39`、`overview.md`（技术栈表 MCP 协议行 + 审计行追加句）、`conventions.md:76`、`build.md`（审计行追加句 + 依赖表 `:133` + 对照检查 `:166`）全部改回 SDK `0.3.4` 事实，`log.md` 09-20 摘要条目同步更正为"godot-cpp 升 10.0.0-stable；mcp-cpp-sdk 维持 0.3.4（0.3.5 尝试后回滚）"，上述带 frontmatter 页 `timestamp` 同步真实时间。
- **更正说明**：本批更正只回写现行事实口径，21:56 节原文条目与 09-17 历史记录一律不改写，留作升级尝试的历史留痕。
- **时间真实性**：本节时间为真实时间 2026-09-20 22:12 +08:00；历史日志零改写。

## 2026/09/20 22:33

- **「可重放监控 + 双目录持久化」批次最终验证与收尾（分支 `feature/observable-traces`，未提交）**：一次性 GUI/脱敏探针 `tests/config/99_image_probe.json` 完成使命后删除（删除前复核 `name=="image_probe"`；`git status --porcelain -- tests/config` 仅剩 `?? tests/config/29_trace_persistence.json` 正式用例）；`AGENTS.md` 测试段与知识库段计数同步为实测值（L1 gtest 269→285 / 文件 29→31，`ctest -E "^gda_runner_"` 271→287，L2 28→29 份，注册点 299→316）。以下均为本轮真实运行结果。
- **L1 全量**：`ctest --preset debug -E "^gda_runner_"` 287/287 全过（含新增 16 用例：`trace_recorder_test` 11 + `log_persist_test` 5）。
- **L2 全量（依赖升级后回归）**：29/29 全过（707.95s）；依赖 godot-cpp 10.0.0-stable + mcp-cpp-sdk 0.3.4。
- **脱敏开关端到端两次探针实测（临时探针，已删）**：默认 true 时 `write_file` 5000 字符入参的 `args_digest` 截断（`truncated=true`、`len≤4200`）；config `desensitize:false` 时放宽不截断（`truncated=false`、`len>4200`）。
- **GUI 图片落盘实测（临时探针，已删）**：`capture_editor_viewport` 事件 `image_ref=images/trace-<session>-<span>-cap.png`、`image_bytes=108`、`image_hash` 非空、宽高 2x2（GUI 启动初期视口）；`user://godot_autopilot/traces/images/` 生成 PNG；trace jsonl 全文无 PNG base64（`iVBORw0KGgo` 零命中）。
- **跨线程 trace 链路实测**：同一 MCP 调用中 `system_status`（main）的 `parent_span` 精确等于 `call_tool`（non-main）的 `span_id`，`trace_id` 相同，`session_id` 与 `session_start` 一致。
- **Dock 新控件存在性实测（GUI 探针 step3）**：`Desensitize data` / `Detail` / `Open Logs` 均存在。
- **时间真实性**：本节时间为真实时间 2026-09-20 22:33 +08:00；历史日志零改写。

## 2026/09/20 23:12

- **构建项目知识库——二轮一致性审计（分支 `feature/observable-traces`，未提交）**：审计范围 `docs/wiki/` 15 个内容页 + changelog 3 页，对照 `git diff HEAD`（实测 42 个已跟踪修改 + 9 个未跟踪）与源码逐项核实。
- **语义冲突消解（3 处）**：[core.md](../modules/core.md) 的 LogPersist 消费方表述——`tool_invoke`/`dispatch` 只做 `TraceRecorder::record` + `session_id` 回显，`enqueue_trace` 外部调用仅 `main.cpp`/`init_session`；[tool_base_design.md](../tool_base_design.md) 图片收集门槛——`data` 需 `format=="png"`，`diff_image_data` 为字符串即收集；[security_contract.md](../security_contract.md) + core.md 图片字段补 `image_width`/`image_height`（脱敏开时 `image_ref` 为空）。
- **缺失事实补录（5 处）**：22 处 `ScopedTraceContext` 包装点全量口径（resource_handlers 8 / debugger_resources 7 / autopilot_tools 3 / code_exec_ops 2 / register_all 1 / dispatch 1，resource 页与 tool_base_design 两页互链）；`get_plugin_log` 响应不含 `detail` 且 since_index 分支只匹配 message（与 `LogSystem::query` 的 message∪detail 口径差异）；`PluginConfig` 失败日志 `log_detailed`（path=/reason=）；`AutopilotTools` 注册/注销/set_enabled 的 `log_detailed` detail 口径。
- **行号漂移修正（28 处）**：core.md 5、entry_runtime.md 4、tests.md 5、build.md 3、overview.md 4、support.md 5、tools_registry.md 1、tools_ops_b.md 2（含 `CMakeLists.txt:171`、`godot_process.cpp:513-519`、`tests/CMakeLists.txt:130`、`register_all_test.cpp:131-148`、`main.cpp:302-369`、`server_context.cpp:249-253`、`debugger_ops.cpp:411`、`runtime_game_ops.cpp:266` 等）。
- **附加一致性修正**：support.md 断链 `security_contract.md` → `../security_contract.md`；entry_runtime.md 步骤级 try/catch 四组→五组（含 AutopilotTools 单例注册）；support.md McpConfigDock 布局顺序按代码重排（status_label 位置、Generate→Generate Skills→结果 Label→提示）；overview.md `src/tools/` 47→48 个 .cpp、补 `game_bridge_verify.cpp` 与 `scene_verify`；tools_ops_a.md editor_ops 标题口径重写（editor_tools.hpp 32 ToolSpec / editor_ops.cpp 25 handle_，24 绑定 + 1 共用）。
- **本轮收口修正（entry_runtime.md:52）**：`_enter_tree()` 的"每个步骤独立 try/catch"改为"关键 UI/捕获步骤各自独立 try/catch，失败不中断后续；队列/脱敏/持久化初始化、ServerContext 创建与 export_guard 未包裹"（对照 `src/main.cpp:104-228` 实测；任务指向 support.md，但该句实际位于 entry_runtime.md，support.md 无此表述故未改动）。
- **AGENTS.md 维护**：构建段 add_library 示例补三个新 core 文件 + `sanitize_policy.cpp` 不进 L1 说明；测试段 L2 时长 2 分钟→全量约 12 分钟；架构段新增"可观测与持久化"与"Dock/UI"两条高信号事实；知识库段新增两条规则（changelog 历史页不回溯补链、数值口径以 [tests.md](../tests.md) 数值核算总表为准）。
- **timestamp**：11 个内容页 frontmatter 同步真实时间（core / entry_runtime / tool_base_design / tools_registry / security_contract / tests / build / overview / support / tools_ops_a / tools_ops_b）。
- **未消解项（注明）**：tool_base_design/core 中 `FileAccess::READ_WRITE` 不创建文件属引擎行为断言（实测来源，官方文档未明示 READ_WRITE 创建语义）；tools_ops_a.md 与 AGENTS.md 的行号历史链按"历史留痕不改写"保留。
- **时间真实性**：本节时间为真实时间 2026-09-20 23:12 +08:00；历史日志零改写。
