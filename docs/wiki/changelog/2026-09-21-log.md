# 2026-09-21 维护日志

## 2026/09/21 01:24

- **可重放监控与日志系统批次（分支 `feature/observable-traces`，未提交）**：新增 `src/core/monitor.{hpp,cpp}`（统一埋点门面，纯 std，可被 MCP SDK worker 线程调用）、`src/core/monitor_env.cpp`（`environment_snapshot()`，仅主线程）与 `src/core/perf_sampler.{hpp,cpp}`（主线程周期采样 + 请求超时看门狗）。
  - `monitor`：`emit/tool_call/lifecycle/data_flow/ui_action/security/state/error_event/perf` 写入便捷函数、`begin_request/end_request/note_protocol_error/note_notification/note_client/note_transport` 协议侧钩子、`RequestRegistry`（request_id→trace/span 关联表，上限 4096、FIFO 淘汰）、`RequestScope/push_request/pop_request/current_request_id`、`Counters/counters()`、`sanitize_field/json_escape/build_attrs`、`request_id_text(RequestId)`、`persist_health`、`LockProbe`（RAII 计时，锁等待超过阈值才记录，否则不产生事件）。
  - `monitor_env`：会话开始写 `kind=snapshot` 事件（gda_version / Godot 版本 / OS 名与版本 / 处理器数 / 对象数 / 编辑器模式 / 场景路径与根节点 / port / desensitize / show_time / allow / session_nonce）。
  - `perf_sampler`：主线程周期采样（默认 5s，env `GODOT_AUTOPILOT_PERF_INTERVAL_MS`）写 `kind=perf` 事件（帧时间均值/最大、FPS、近似 cpu_frame_time_ms、内存、对象/节点数、队列深度/拒绝数/锁等待、吞吐/错误率/平均延迟）；请求超时看门狗（默认 60s，env `GODOT_AUTOPILOT_REQUEST_TIMEOUT_MS`，超时发 `kind=execution_state, state=timed_out`）；`set_queue(const CommandQueue*)` 由 `main.cpp` 注入。
- **trace / 日志模型扩展**：`trace_recorder` 新增 `enum class TraceKind`（15 值：tool_call/protocol_request/protocol_response/protocol_error/protocol_notification/lifecycle/data_flow/execution_state/concurrency/perf/snapshot/error/persist_health/ui_action/security）与 `trace_kind_name()`，`TraceEvent` 追加 `kind/name/request_id/correlation_id/phase/state/monotonic_ns/thread_id/attrs/error_type/stack/bytes`（`to_json_line` 在原键序之后追加，即 jsonl schema v2、旧键保留），敏感键剥离改为大小写不敏感通用集合（data/base64/script_content/token/secret/password/passwd/key/api_key/apikey/authorization/credential/cookie/session_token 及 `*_token`/`_key`/`_secret`/`_password` 后缀），新增 `monotonic_now_ns/current_thread_id/sanitize_text/query_by_request`；`log_system` 的 `LogEntry` 追加 `trace_id`/`span_id`、新增 `log_detailed` 6 参重载（旧 4 参委托）、`query` 的 `filter_text` 同匹配 message/detail/trace_id/span_id；`log_persist` 新增 `dropped_log_lines/dropped_trace_lines/bytes_written/flush_failures/rotations/pruned_files` 计数与 `health_summary()`/`trace_dir()`，flush 用 `LockProbe`（阈值 10ms），缓冲溢出/写失败/rotate/prune 均发 `kind=persist_health`；`command_queue` 新增 `Stats`（submitted/executed/rejected_closed/rejected_full/dropped_on_close/lock_wait_ns/pending/capacity）与 `stats()`。
- **协议请求端到端关联**：`server_context.cpp` 接入 `opts.on_request`（`monitor::begin_request`，含 `_meta` 的 traceparent 提取）与 `opts.on_notification`；响应侧改用 `opts.outgoing_filters`（`mcp::FilterPipeline` + `MessageFilterFuncAdapter`）捕获出站 `JsonRpcResponse`/`JsonRpcErrorResponse` 以完成 `end_request`/`note_protocol_error`（因 SDK 的 `on_response` 仅用于「服务端发起请求的应答」，不覆盖我方回复客户端的响应），`on_client_connected/on_transport_close/on_transport_error/on_protocol_error` 追加 `note_client/note_transport/error_event`，start/stop/restart/失败均发 lifecycle/error 事件；`register_all.cpp` 元工具处理器用 `RequestContext::GetRequest().id` 建立 `monitor::RequestScope` + `tools::RequestSpanGuard`（新增 lifecycle：`registry_build`/`registry_refresh_derived`/`registry_refresh_dynamic`/`tools_registered`），`tool_invoke` 的 `TraceContext` 增 `request_id`、新增 `RequestSpanGuard`、`record_invoke_trace` 走 `monitor::tool_call`，`dispatch::call_handler` 捕获/恢复 request_id、handlers 互斥锁用 `LockProbe`（阈值 5ms）；`system_status` 返回新增 `tool_count` 与 `queue_depth`（工具总数仍 399，未新增工具）。
- **入口与 UI**：`main.cpp` 的 `_enter_tree` 顺序新增 `monitor::lifecycle("plugin_enter_tree")` → `perf_sampler::set_queue(&queue())` →（`s_queue.open()` 之后）`init_session()` → `monitor::environment_snapshot()`，`_process(delta)` 先 `perf_sampler::tick(delta)` 再 `flush_on_main_thread()`/`drain()`；新增 lifecycle `tools_singleton_registered`/`server_ready`/`plugin_ready`/`plugin_exit_tree`/`scene_level_initialized`/`editor_level_initialized`/`editor_level_terminated`/`scene_level_terminated`，server 启动失败发 `error_event`；McpLogDock 新增 Open Traces 按钮（打开 `user://godot_autopilot/traces`）与 Trace toggle 视图（日志行带可点击 `[trace]` 标记、按 trace_id 渲染结构化事件列表、搜索兼匹配 detail），两 dock 交互回调发 `monitor::ui_action`（脱敏与 allow 变更另发 `monitor::security`）；资源读取发 `data_flow("resource_read", bytes, {uri, request_id})`/失败 `error_event`，用户工具 register/unregister/set_enabled 发 lifecycle，游戏任务过期/丢弃/迟到响应发 `monitor::state(..., "degraded"/"discarded", true)`。
- **构建与测试（实测）**：`add_library` **86→89**（`src/core/` **12→15**），`tests/CMakeLists.txt` 的 `GDA_UNIT_BUSINESS_SOURCES` 新增 `src/core/monitor.cpp`（`monitor_env.cpp`/`perf_sampler.cpp` 不进 L1）；L1 **285→312 gtest**（unit 文件 **31→33**：新增 `monitor_test` 9 / `perf_sampler_test` 6，`trace_recorder_test` 扩至 18、`log_system_test` 扩至 9、`log_persist_test` 扩至 8）；L2 **29→30 份**（新增 `30_observability`，3 stage：`system_status` 断言 `tool_count`/`queue_depth`；扫描 traces jsonl 断言 `kind=lifecycle`/`snapshot`/`protocol_request` 与新字段；扫描 logs 断言含 `trace=` 的 detail 行）；ctest 注册点 **316→344**（L1 过滤 `-E "^gda_runner_"` 287→314）；工具总数 399 不变、`SchemaStatisticsBaseline` 不变。
- **实测验证结论**：L2 会话中 trace jsonl（schema v2）观测到事件种类 `lifecycle`/`protocol_request`/`protocol_response`/`tool_call`/`persist_health`/`perf`/`snapshot`/`concurrency`；`tools/call` 请求的 trace_id/span 与 `call_tool`、域工具 span 正确嵌套，`request_id` 贯通，`protocol_response` 回填 `duration_ms`。
- **wiki 同步（11 页 + `tests/README.md` + `AGENTS.md`）**：核心 / 入口与运行时 / tool_base_design / tools_registry / support / security_contract / conventions / tests / build / overview / index 与 `tests/README.md`、`AGENTS.md`；详见 [tests.md](../tests.md)、[modules/core.md](../modules/core.md)、[security_contract.md](../security_contract.md)。
- **时间真实性**：本节时间为真实时间 2026-09-21 01:24 +08:00；历史日志零改写。
