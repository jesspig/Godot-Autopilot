# 2026-08-17 维护日志

## 2026-08-17-02

**移除工具栏状态栏，连接状态移入配置面板**：

- 删除 `src/ui/mcp_status_bar.{hpp,cpp}`（右上角 `CONTAINER_TOOLBAR` 连接显示），`main.cpp` 同步移除创建/更新/清理/ClassDB 注册
- 连接状态显示由 `McpConfigDock` 承担（"Running on port N" / "Server offline"，主题色标注）；删除无调用者的 `set_on_port_changed` 回调机制
- 验证：编译 + ctest 77/77 全过；文档 overview/entry_runtime/support 同步

## 2026-08-17-01

**配置面板交互调整（单选下拉 + 防覆盖强化）**：

- 客户端选择由 8 个 CheckBox 多选改为 `OptionButton` 单选下拉，Generate 只生成选中客户端的配置文件
- 生成前必先解析现有配置：JSON 保留其他键仅更新 `godot-autopilot` 条目；Codex TOML 已含 `mcp_servers` 段或 JSON 无法解析时跳过不写，避免覆盖用户的 agent 配置
- 验证：编译通过、L1 11 用例全过；dll 部署待用户手动（编辑器运行中占用）

**知识库全量复核（工作树无代码变更，纯 wiki 维护）**：

- **11 个概念页面全部新增 YAML frontmatter**（`type`/`title`/`description`/`tags`/`timestamp`/`resource`；`index.md`/`log.md`/changelog 按保留文件规则不加）；timestamp 均为 2026-08-17T01:03:27+08:00（真实系统时间）
- **数值修正**：build.md `add_library` 68→**70** 个 .cpp（core 6→7 含 plugin_config、util 4→5 含 client_config_gen）；index.md gtest 61→**72**、A 组工具 180→**172**；overview.md 目录结构（tools 36→41 个 .cpp、补 PluginConfig/client_config_gen、gtest 61→72）；tools_registry.md 遍历步数残留 408 统一为 **410**；core.md 文件数 15→**16**；support.md util 7→**6** 组、prompt_tool_usage 685→**625** 行、删除已移除的 `on_port_changed` 回调描述（改述为面板内状态 Label）；entry_runtime.md main.cpp 343→**331** 行、game_bridge_eval.cpp 445→**468** 行；conventions.md 工具名 331→**332**；example.md project.godot 36→**32** 行、`assembly_name` 值 `新建游戏项目`→`Example`
- 审计日期同步：overview/build/tests/example/core/tools_registry/tools_ops_a/support 均更新至 08-17 并标注原因；无日期头页面（conventions/entry_runtime/tools_ops_b）不新增
- 验证：git diff 逐项核查工作区变更（相对 HEAD 无代码改动）；CMake 源 70 个逐行计数；gtest 72 = 9 文件逐文件统计（14+13+10+7+7+6+11+3+1）；project.godot/docs/assets 实测

**AGENTS.md 与 conventions.md 流程修正（源码无改动，纯文档对账）**：

- **添加工具流程修正**：AGENTS.md/conventions.md 原第 3 步"手改 `register_all.cpp` 注册 `g_handlers` + `catalog.add_tool`"为过时描述——实测 `register_all.cpp:286-289/569-573` 把 `tool_defs.def` include 两次，`TOOL_ENTRY` 宏自动写入 `g_handlers` 与 `ToolCatalog`；修正为"def 加 TOOL_ENTRY 行，register_all.cpp 无需手改"，并补充 SCHEMA_BASIC 需同步 `schema_*_ops.cpp` fill 表（`build_schema_for` 按名查表，SCHEMA_NONE 走 `build_schema_for_none_by_name` 回退）、元工具不进 def 的例外
- **端口解析优先级补全**：环境变量 `GODOT_AUTOPILOT_PORT` > `user://godot_autopilot/config.json`（`PluginConfig`）> 默认 9527
- **新增 CRLF 伪变更陷阱**：`git status` 大量 M 但 `git diff HEAD` 为空（LF→CRLF 规范化），改动前必须用 `git diff` 确认真实变更
- **工程原则精简**：删除与全局智能体准则重复的 KISS/DRY/YAGNI/整洁可读条目，保留仓库特有"不写注释、代码即文档"与"先搜索后动手"
- 验证：register_all.cpp 宏定义/展开位置、schema fill 表、端口优先级（`ServerContext::resolve_port`）逐一核对；AGENTS.md 全部数值（339/343/72/410/34/208/124/23 类）无变化

## 2026-08-17-00

**新增右侧栏 MCP 配置面板（`McpConfigDock`）**：

- 端口管理：SpinBox + Apply → `ServerContext::restart(port)` 运行时重启（无需重启编辑器），成功后持久化到 `user://godot_autopilot/config.json`（新 `PluginConfig`）；端口解析优先级调整为 环境变量 > user:// 持久化值 > 默认 9527（环境变量优先保证测试/CI 不受面板配置影响）
- 一键生成客户端配置：8 个工具（opencode / Claude Code / Codex / Cursor / GitHub Copilot / Trae / Qoder / WorkBuddy）各自独立配置文件（`opencode.json` / `.mcp.json` / `.codex/config.toml` / `.cursor/mcp.json` / `.github/mcp.json` / `.trae/mcp.json` / `.qoder/settings.json` / `.workbuddy/mcp.json`），URL 统一 `http://127.0.0.1:<port>/mcp`；JSON 已存在时智能合并（保留其他键，仅更新 `mcp`/`mcpServers` 下的 `godot-autopilot` 条目），Codex TOML 已含 `mcp_servers` 段时跳过
- 新增纯函数生成器 `src/util/client_config_gen.{hpp,cpp}`（仅依赖 std + `mcp::JsonValue`，无 Godot API），11 个 L1 用例覆盖渲染快照/合并三态/TOML 追加与跳过
- 验证：`uv run build.py` 构建部署成功；ctest 77/77 全部通过（L1 72 + L2 5 runner）；`Example/project.godot` 测试残留已还原
- 文档：core.md（ServerContext.restart + PluginConfig + 端口优先级）、support.md（McpConfigDock + client_config_gen）、tests.md（72 gtest 核算）、AGENTS.md（L1 计数）同步