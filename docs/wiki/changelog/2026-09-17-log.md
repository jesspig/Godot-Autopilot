# 2026-09-17 维护日志

## 2026-09-17-00

- **代码精简批次（feature/cleanup-dead-code-docs，纯删除不改行为）**：全分支 27 个文件 +104/−592（净删 488 行），运行时行为零变化——域工具 385 / 非 SIDE 325 / SIDE 60 / 元工具 7 / catalog 393 全部不变。构建实测 `cmake --preset debug` 65/65 成功、L1 `ctest --preset debug` **243/243 全绿**。
- **死代码删除**：`src/core/editor_coords.{hpp,cpp}` 删仿射变换子集（`make_affine`/`compose`/`transform_point`/`invert`/`transform_rect`/`rect_contains`/`rect_intersect`/`layout_marks`/`Mark`/`Point`/`Size`/`Affine` 共 114 行；`Rect` 因系存活类型 `DiffResult::bbox` 的字段类型保留并移至其声明旁）；`src/tools/tool_catalog.{hpp,cpp}` 删旧式默认目录 `populate_default_tools()`（93 行，生产路径从不生效，catalog 完全由 ToolRegistry 经 `replace_tools` 动态填充）。存活函数 `viewport_point_to_window`/`viewport_rect_center_to_window`/`fit_within`/`scale_size`/`diff_sample` 及调用点 `game_bridge.cpp`/`capture_ops.cpp` 零触碰。
- **测试同步缩减（L1 267→243）**：`editor_coords_test` 37→15（删仿射/矩形/layout_marks 用例 22 项，保留 FitWithin 4/ScaleSize 5/DiffSample 6）、`vision_assist_test` 8→7（`RegionCropIsTopLeftIntersection` 改用文件内局部辅助保持验证语义；`NodeMarksShareBudgetWithoutOverlap` 因 `layout_marks` 从未接入真实标注链路、属伪测试而删除）、`tool_catalog_test` 7→6（删 `PopulateDefaultToolsOnce`）。unit 文件数仍 25，ctest 注册点 292→**268**（`ctest -N` 实测）。
- **零风险微清理**：删 `LogEntry::operator->()`（零引用）；`nav_ops.cpp` 删匿名命名空间内 `vec2_to_json`/`vec3_to_json` 局部副本、5 个调用点改显式 `util::` 限定（消除与 `util/json_godot.hpp` 的重复实现）；`tests/runner/main.cpp` 的 `#error` 文案旧目标名 `gda_engine_tests`→`gda_test_runner`；`tests/CMakeLists.txt` GLOB 注释更新至现状；`Example/project.godot` 删除误入库的空 `test` input action；`.gitignore` 删 5 条防御性条目（`_build_/`/`_download/`/`tests/*.xml`/`report-*.json`/`test-results/`，对应路径均不存在；`tests/output/` 原已存在）。
- **技能册模板同步（384→385）**：`src/util/skill_templates/autopilot.md` 3 处与 `autopilot--tool-catalog.md` 1 处域工具计数 384→385；catalog Capture 分节修正为 `capture_editor_viewport` + `review_scene_visually`（后者此前全模板体系零收录），`get_editor_viewport_geometry` 条目移入 Editor 分节（计数 30→31，补条目）。
- **文档数字同步**：`plans/roadmap.md`（"8 客户端"→已交付 20 客户端）、`security_contract.md`（ModifiesWindow 19→20、`game_runtime` 名单补 `start_game_job` 共 7）、`build.md`（add_library 79→83 个 .cpp、core 8→9、tools 47→50、行号 65-144→65-148、删"README 差 1"表述）、`overview.md`（README 已同步 ~385 精确一致）、`modules/tools_ops_b.md`（残留行 9/9→10/12）、`modules/support.md`（register_tools 行号 214-220→227-232）、`modules/tools_registry.md`（`populate_default_tools` 已删除表述，catalog 无默认填充）、`tests/README.md`（config 17→25 份补全清单、324/384→325/385、L1 229→243）、`index.md`/`tool_base_design.md`/`tests.md`/`AGENTS.md`/`overview.md` 目录树/`plans/roadmap.md` 引言（L1 267→243、ctest 292→268 全库对齐，268 为 `ctest -N` 实测）。**白名单维持 176 条不改**（`kManualWhitelist` 引号配对复核恰 176，前期分析报告的"180"系误报，文档与代码本就一致）。受影响页面 `timestamp`/审计日期头同步 2026-09-17。
- **已知遗留**：`Example/project.godot` 工作副本存在 LF→CRLF 警告（仓库已知 CRLF 现象）。
- **勘误（00:40）**：初版曾将 `.gitignore` 的 `report/*` 一并删除（判定"无生成方"）。经复查，`report/` 为历史会话人工产出的端到端测试报告文档（`git log -S "report/"` 证实任何版本代码均未写入该路径，但文件本身是 09-13~09-16 的开发验证记录），该条目的作用是防止本地会话文档被误提交而非忽略构建产物——已恢复 `report/` 条目并加注释说明。

## 2026-09-17-03

- **客户端配置写入修复**：`src/ui/mcp_config_dock.cpp` 的 `write_file` 在打开文件前先 `DirAccess::make_dir_recursive_absolute(path.get_base_dir())` 递归创建缺失父目录——修复 14 个含目录路径客户端（`.zcode/`、`.cursor/` 等）在目录不存在时首建失败的问题；根级路径与 `_on_generate_skills` 既有建目录逻辑均幂等无回归。
- **pi 路径更正**：pi (pi-mcp-adapter) 配置路径 `.pi/mcp.json`→`.mcp.json`（依据 pi-mcp-adapter 官方 README，`/mcp setup` 项目级配置默认在项目根），与 Command Code、CodeBuddy 共用同一路径（merge 幂等）；`client_config_gen.cpp` kClients 表条目、`client_config_gen_test.cpp` 路径断言、`AGENTS.md` 注记、`modules/support.md` 客户端表同步，`support.md` 的 `_on_generate` 行为描述补充"父目录不存在时先递归创建"。
- 验证：`cmake --preset debug` 构建成功，L1 `ctest --preset debug -E "^gda_runner_"` **243/243 全绿**（03 时实测）。

## 2026-09-17-22

- **文档同步批次（T09+T12-docs，只改文档零代码）**：同步工作树未提交实现（`git diff HEAD` 实测 15 文件 +338/−62）对应的 7 项行为到 wiki，另同步依赖/兼容版本口径；未改 `src/tests/cmake/build.py`，未执行构建。
- **P12 登记状态如实返回（`modules/tools_ops_a.md` editor_ops）**：`save_editor_scene_as` 删除 close+reopen fallback 与 `tab_rebuilt` 字段；GDExtension 无页签登记 API（`set_scene_file_path` 只改节点属性），未注册时如实返回 `tab_path_registered:false` + `warning`。
- **P4 多签秒级失败（A 组 editor_ops + `modules/entry_runtime.md` 2.4）**：`create_editor_scene` 的 `close_current` 仅关当前签，关闭后编辑根仍非空即报邻签占用 + `editor_state`，不进 `add_root_node` + 轮询空转至超时；工具/ schema 描述同步"仅关当前签"。
- **P5 跨帧 undo 与选中清理（A 组 scene_ops）**：`delete_scene_node` 的 undo 按原生 SceneTreeDock 顺序并经 `add_undo_reference` 保活、可跨帧撤销；删除后从编辑器选中集摘除节点。
- **P8 改脚本工作流（A 组脚本小节 + B 组运行时小节 + `create_script` 注记）**：改完运行中游戏所用脚本先调 `reload_game_scripts` 再 `reload_current_scene`/重试，单重载场景不保证重读（`CACHE_MODE_REUSE`）。
- **P2 释放立即 flush（B 组运行时小节 + `modules/entry_runtime.md` 3.2）**：游戏侧 `DelayedRelease` 到期释放与动作释放恒 `flush_buffered_events`，抬起事件不再滞留缓冲。
- **P3 求值启发式与明确错误（`modules/entry_runtime.md` 3.3）**：`call_method` 按方法签名逐参推导类型、无提示对象走 Color/Vector2/3/4 启发式转换；方法缺失与引擎报错返回 null 均转明确错误（附查询指引/`error_details`）。
- **P7 文本点击与序号口径（B 组运行时/截图小节）**：`click_game_ui_element` 支持 `text`/`text_index`（path 优先；先精确后子串；歧义/越界报错附候选），`annotate` 的 `id` 为单次捕获序号、不可点击（未知参数错误连带提示）。
- **版本口径（`build.md:100,133,166`、`conventions.md:73`、`overview.md:101-102`、`AGENTS.md:32`）**：`compatibility_minimum` "4.3"→"4.7"；godot-cpp 10.0.0-rc1→rc2（新增 `GODOTCPP_API_VERSION "4.7"` FORCE 覆盖旧缓存）；mcp-cpp-sdk 0.3.3→0.3.4（以 `cmake/FetchDependencies.cmake:17-29` 实测为准）。
- **时间真实性**：本节时间为真实时间 2026-09-17 22:12 +08:00；历史日志（09-13 等）零改动；改动页 `timestamp`/审计日期头同步 2026-09-17。
- **勘误**：P12 初版删除 `save_editor_scene_as` 的 close+reopen fallback 导致 `07_scene_tabs`/`13_inline_subresource` 回归（L2 实证 07 10/16、13 35/36：无 reopen 时存盘后标签页仍登记旧路径，`get_unsaved_scenes` 报空路径、`reload_editor_scene` 报 scene is not open），已恢复 fallback 与 `tab_rebuilt` 字段（重建失败才附 `warning`）；GDExtension 无页签登记 API，close+reopen 是唯一同步手段，页签焦点变化为正确性必要代价。

## 2026-09-17-23

- **P3 错误通道升级（[入口与运行时](../modules/entry_runtime.md) 3.3）**：`call_method` 改直调 GDExtension 接口取同步 `r_error`（脚本方法经 `object_call_script_method`，`INVALID_METHOD` 回退按 `get_method_list` 的 `id` 取 `MethodBind` 经 `object_method_bind_call`，与引擎 `Object::callp` 同序）；成功与否只看 `r_error`（`void`/`null` 不再误报），错误按 `call_error`/`argument`/`expected` 返回明确错误，错误水印增量仅作上下文附加。依据：`git diff HEAD` 实测 `src/runtime/game_bridge_eval.cpp`（新增 `eval_call_error_result`/`eval_native_method_bind` 等直调链路，删除 `callv` 返回 null + 水印判错分支）。
- **18 号用例语义（[测试体系](../tests.md) L2 表）**：`18_script_freshness` 的 `attach` 断言同步为 `instantiated=false` + `note` 非空——非 `@tool` 脚本在编辑器内 `can_instantiate=false` 系引擎语义，新鲜度以源码 MARKER 与 property 链路为准。依据：`git diff HEAD` 实测 `tests/config/18_script_freshness.json`（`description` 与 `freshness_attach_fresh_script` 期望同改）。
- **P2 三句约束跳过**：`git diff HEAD` 实测 `src/tools/game_tools.hpp` 仅 `queue_game_input` 描述追加三句中文约束（同帧 press+release 边沿/hold 开销/槽位释放）；`docs/wiki` 内 grep `只清自身槽位|固定开销|同时为真` 零命中，[B 组运行时小节](../modules/tools_ops_b.md)与入口页 3.2 仅描述释放 flush 机制、未逐字复述且语义无脱节，按任务规则跳过。
- **时间真实性**：本节时间为真实时间 2026-09-17 23:40 +08:00；历史日志零改写；改动页 `timestamp` 同步 2026-09-17T23:40:00+08:00。未改 `src/tests/cmake/build.py`，未执行构建。
