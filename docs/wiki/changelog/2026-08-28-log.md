# 2026-08-28 维护日志

## 2026-08-28-01

- **日志系统增强（仅日志部分，未涉及 MCP SDK / 环回绑定）**：
  - 编辑器底部日志 dock 标题由 "MCP Log" 改名为 **"GDA Log"**（概述页、support.md 的 McpLogDock 小节、entry_runtime.md 的 `_enter_tree` 步骤三处引用同步）
  - 配置面板新增 "Show timestamps" 勾选：开启时每条日志前缀 `[HH:MM:SS]`（本地时、时分秒），默认开启，经 `user://godot_autopilot/config.json` 的 `show_time` 键持久化；折叠（合并）重复日志时除条数 `(N)` 外**始终**显示最新一条的 `[HH:MM:SS]`，不受总开关控制（support.md McpLogDock 时间前缀小节）
  - `ServerContext` 关键流程（初始化 / 注册工具 / 启动传输 / 停止 / 重启）补充诊断日志；`start()` 启动失败不再硬编码 "unknown exception"，改为输出真实异常类型（core.md ServerContext 小节）
  - `call_tool` 分发增加 Debug 级日志
  - 同步更新 overview / support / entry_runtime / core 四页 frontmatter `timestamp` 与审计日期行为 2026-08-28
- **验证**：文档与代码行为逐条核对（dock 改名两处 set_title、show_time 持久化键、折叠最新时间不受开关控制、启动失败异常类型透传），未编译、未运行
