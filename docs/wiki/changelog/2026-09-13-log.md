# 2026-09-13 维护日志

## 2026-09-13-02

- **mcp-cpp-sdk 0.3.2 → 0.3.3**：`cmake/FetchDependencies.cmake` GIT_TAG 升级，重新 configure 拉取 0.3.3（HEAD `a984cd2`）并通过全量重新编译（mcp-core/transport/protocol/server/client/http 静态库 + 插件 DLL），无 API 适配需求；ctest L1 114/114 全绿；`uv run build.py` 部署通过。0.3.3 相对 0.3.2 的主要变化：官方 conformance suite 接入与 MRTR 服务端链路、Bearer TokenVerifier 与受保护资源元数据端点、FileEventStore 与外部会话恢复、任务化执行与协作取消、URL elicitation 闭环、传输层死锁与会话过期类型化修复、`ElicitResult` wire 键对齐、构建层 NOMINMAX 与 `MCP_BUILD_CONFORMANCE` 开关（本仓库未启用新特性，仅版本升级）。
- **知识库同步**：根 `AGENTS.md` 依赖版本行、`build.md`/`conventions.md`/`overview.md` 的版本引用与 frontmatter timestamp 更新至 2026-09-13。

## 2026-09-13-03

- **反馈修复批次全量交付（P0/P1，含 L2 验证）**：
  - 属性写入静默失败面收敛：`property_set` 支持引擎 NodeType（hint 34，含 C# `[Export]`）节点路径→引用转换（失败报 error）、数组属性逐元素转换（Node 元素路径/`{"__node_ref__": ...}`，资源元素 `res://`/`memory://` 字符串或 `{"path": ...}`/`{"resource": ...}`，`null` 留空槽），非数组值不再静默清空数组，无法安全表达的元素报错；`readback_util::check_readback` 增 `type_sensitive`（Object/数组写入未生效判 REJECTED → `value not applied` error 并尝试恢复旧值）；`variant_json::deserialize_as_object` 补齐 `__node_ref__`（不再实例化游离节点）；`property_get_list` 增 `only_script_variables`/`property_filter`。（`src/tools/property_ops.cpp`、`src/util/type_hint.hpp`、`src/util/readback_util.*`、`src/util/variant_json.cpp`）
  - 资源事务：`save_resource` copy-on-write（`dest_path≠path` 且经 `path` 定位时先深拷贝，响应 `copy_on_write`/`source_path`，源缓存实例不被改写）；新增 `reload_resource`（`CACHE_MODE_REPLACE` 强制刷新，响应 `replaced`/`reused_cached_instance`）与 `copy_resource_file`（逐字节复制 + 编辑器扫描，WritesFile）；`rename_resource_file` 目录参数 fail fast（指引 `move_resource_file`）；目录 move 排除 `.import`/`.uid` sidecar；移动/删除后自动关闭受影响旧路径标签（`closed_tabs`/`closed_tabs_warning`，remove 另有 `open_tabs`）；`resolve_resource` 的 path 加载注册进内存注册表（跨调用存活）；`create_resource`/`get_resource_types` 支持 GDScript `class_name`/C# `[GlobalClass]` 全局类；`duplicate_resource` 增可选 `name` 并注册。（`src/tools/resource_ops.cpp`、`resource_tools.hpp`，计数 24→26）
  - 场景/编辑器可靠性：`create_editor_scene` 增 `timeout_ms`（默认 `GDA_NEW_SCENE_SWITCH_WAIT_MS=2000`，50-30000，非法值报错），失败释放未接管节点并返回 `waited_ms`/`timeout_ms`/`node_released`/`editor_state`；`save_editor_scene` 失败返回 `error`；`save_editor_scene_as` 成功返回 `result:"saved"` + `tab_path_registered`/`tab_rebuilt`（Godot 4.7 `save_scene_as` 不更新标签路径，close+reopen 修复）；`reload_editor_scene` 未打开场景报 error、成功返回 `{result:"reloaded", scene_path, observed?}`；`open_editor_scene` unsaved 错误补恢复指引。（`src/tools/editor_ops.cpp`、`schema_editor_config_ops.cpp`）
  - 脚本/调试器描述修正：`attach_script_to_node` 放宽为任意 Script 资源并注明替换不迁移导出引用（`node_paths` 可能悬空）；`get_debugger_errors`/`get_debugger_output`/`get_debugger_scene_tree` 描述改为"无 session 返回空 + note，**无编辑器回退**"，编辑器侧统一 `get_debugger_log`；`get_game_input_status` 不再返回 `recent_engine_errors`。（`script_tools.hpp`、`debugger_tools.hpp`、`schema_debug_sys_ops.cpp`、`game_tools.hpp`）
- **截图经 `call_tool` 以 MCP image content 交付**：新增 `src/util/mcp_image_content.hpp`，`register_all.cpp` 的 `call_tool` 回调对 `capture_editor_viewport`/`capture_game_viewport`/`capture_display_screen` 附加 image 块（文本 JSON 的 `data` → `"<attached-as-image-content>"` + `image_attached:true`，其余字段保留）；`batch_execute`/`code_execute` 内不附加（完整 base64）；L1 新增 `tests/unit/mcp_image_content_test.cpp`。
- **技能 7→8 册**：新增 `godot-autopilot-csharp`（`csharp.md` + `csharp--tool-reference.md`：C# 编译循环、`[GlobalClass]`、`[Export]` 属性、无热重载）与总纲 `autopilot--development-workflow.md`（改→跑→观察→诊断→修→复验闭环）；`SKILL_COUNT=8`，registry 8 条，`skill_templates/` 31 个文件（30 .md + registry.json），22 个 references；`skill_gen_test` 名单改 `AllEightSkillsPresent`。
- **测试基线与计数口径**：L1 114/114、L2 8/8（新增 `tests/config/07_scene_tabs.json`；`03_tools_contract` 增 `reload_resource` 缺参断言）；365 域工具 = 316 `GDA_TOOL_CLASS(` + 49 `GDA_TOOL_CLASS_SIDE(`，372 = 365 域 + 7 元，373 = 372 + `system_status`（catalog/index 全量）。

## 2026-09-13-17

- **游戏运行通道根因修复（关键架构）**：MCP 注册 lambda 中 `call_tool` 不再经 `queue.execute_sync` 在主线程执行——`register_all.cpp` 对 `call_tool` 直接在当前（MCP）线程执行 `tool->execute(args)`（其余 6 个元工具仍走 `execute_sync`）。此前 `wait_pending_response` 在主线程等待（最长 30s）阻塞 `_process()` 消息泵，导致调试器消息停摆、所有 `game_runtime` 工具必然 7s 超时（现场已复现并修复验证）；领域工具 handler 仍由 `dispatch::call_handler` 路由回主线程，截图定型读盘经 `queue.submit`。
- **运行时通道可观测性（P0 建议落地）**：
  - 新增 `get_plugin_log` 工具（Debugger 类；域工具 365→366）：读插件进程内 LogSystem 缓冲，参数 `limit`（默认 100/上限 1000）/`level`/`category`/`filter`/`since_index`，返回 `entries`/`count`/`next_index`；`call_tool` 描述同步改为 "367 non-meta tools"（366 域 + `system_status`）。
  - `debugger_ops::_capture` 对 `gda:ready`/`gda:response` 均返回 true（修复编辑器逐条打印 `Unknown message: gda:response` 的噪声）；会话首次 ready 触发 `runtime_ops::run_channel_self_check` 后台 status 往返自检（成功/失败写插件日志，可经 `get_plugin_log` 读取）。
  - `handle_game_response` 的静默失败出口（非对象载荷、缺 `request_id`）写 Warning 日志；超时文案附广播会话数、收到响应数、最近响应年龄与 pending 数；`debugger_broadcast_request` 新增会话数回传。
  - `game_bridge::register_listener` 注册 `gda` 消息捕获前后各查一次 `has_capture`，失败记 System 错误并 return（不挂日志器、不发 `gda:ready`）。
- **类型转换严格化与静默失败收敛**：新增 `VariantJson::deserialize_strict`（Rect2/Rect2i 的 `size` 支持 `w/h` 与 `x/y` 别名、冲突报错；AABB 同理 `w/h/d`；Transform2D 必须 `columns`；Transform3D 必须 `basis.rows`；违规抛 `std::runtime_error`），`property_set`/`create_scene_node` 属性路径改用严格版；dispatch 捕获 `std::exception` 时透出 `ex.what()`（此前一律报 unexpected C++ exception）；`readback_util::check_readback` 对 16 类值类型做分量近似比较（设置未生效即回读等于旧值判 REJECTED）；`create_scene_node` 增 `property_warnings` 且属性异常时回收已挂载节点；`save_resource` 的 `verified` 改用 CACHE_MODE_IGNORE 磁盘重读，描述补 safe-save 说明；`set_resource_property` 与运行时 eval `set_property` 的读回启用 `type_sensitive`。
- **授权门持久化与即时启用**：解析优先级 `GODOT_AUTOPILOT_ALLOW` 环境变量 > `user://godot_autopilot/config.json` 的 `allow` 键 > 默认拒绝；新增 `PluginConfig::load_allow/save_allow` 与 AllowProvider 桥接（每次工具调用实时读取）；MCP Config dock 新增 "Allow code_execute" 复选框（写配置、下一次调用生效，环境变量存在时优先）；拒绝响应增 `enable` 字段并写 Warning 日志。
- **工具与文档细节**：`intersect_physics_2d_ray` 返回 `space_rid`/`auto_detected` 诊断字段并写明编辑器/游戏物理世界相互独立；`capture_editor_viewport target=game` 改为非阻塞 pending 协议（删除 `runtime_ops::game_capture_blocking` 死代码）；`create_spriteframes` 增 `default_animation` 参数；`set_tilemap_cell(s)` 文档写明 `source_id:-1` 擦除语义并删除 "~64 条截断" 错误表述；`get_editor_settings` 返回 `hint`/`hint_string`/`enum_options`；鼠标注入与 `warp_display_mouse` 的窗口客户区坐标基准写入工具描述、内置提示词与技能（keycode-reference/tool-usage/input-injection）；运行时通道失败的降级指引（错误响应附 `hint`）与日志四路径写入技能文档。
- **测试**：L1 114→126（新增 `tests/unit/variant_json_strict_test.cpp` 12 项严格形状与完整消息断言）；L2 8→9 份（新增 `tests/config/08_property_readback.json`；`03_tools_contract` 枚举 316→317）；ctest 注册点 122→135；L2 运行需 `GODOT_AUTOPILOT_ALLOW`（`00_meta`/`04_resources_scripts` 的脚本执行用例）；`mcp_test_client` 增一次 transport 错误重试。
- **已知遗留（待跟进，勿按已修复处理）**：mcp-cpp-sdk **客户端**偶发响应等待缺陷——runner 场景下特定响应约 50% 触发约 60s 等待，同一时刻服务端对独立请求完全健康；已通过移除触发步骤 + L1 补断言在测试侧规避，SDK 侧根因未修复。
- **知识库同步**：index/overview/security_contract/tests/tool_base_design/conventions/modules/tools_registry/modules/tools_ops_a/modules/tools_ops_b/modules/core/modules/entry_runtime/modules/support/plans/roadmap 更新工具计数（366 域/373 可达/374 全量）、L1 126/L2 9/ctest 135、授权门、严格 JSON 形状、运行时通道修复与 `call_tool` MCP 线程例外；受影响页面 frontmatter timestamp 同步为 2026-09-13T17:34:29+08:00。

## 2026-09-13-18

- **代码-文档一致性审计（A 组 8 页 / B 组 7 页）**：两组逐页核对 `docs/wiki/` 页面并修复行号/计数/目录结构/过时引用（A 组含 `example.md` 整页重写：旧 6 篇文档结构 → 11 篇 pitch/gdd/art/tech/test 索引）。
- **跨组复核纠正（类别分布口径）**：以工具 `category` 字段实测为准——`physics_tools.hpp` 48 个工具中 `get_debug_object_info` 归类 `Debug`，故 Physics 47 / Debug 16（`debug_tools.hpp` 15 + 1）/ Debugger 6，27 类合计 366；修正 `overview.md` 类别表（Physics 48→47、Debug 15→16）与 README.md / README_zh.md（Debugger 5→6、总量 ~365→~366，含发现机制表与特性标题）。
- **源码文案**：`src/ui/mcp_config_dock.cpp:292` 技能按钮 tooltip "Write the 7 godot-autopilot skills" → "Write the 8 godot-autopilot skills"（对齐 `SKILL_COUNT=8`）。
- **清理示例工程测试残留**：删除 `Example/gda_tmp_rename/`（L2 `05_rename_references` 临时产物）与 `Example/addons/godot-autopilot/~godot-autopilot_0.pdb`（热重载备份）；`example.md` 改注为"运行 L2/编辑器热重载可能生成、可安全清理"。
- **审计结论**：A/B 模块清单合计 180+186=366 复核通过；L1 126/L2 9/ctest 135 与 `AGENTS.md` 测试段一致。
- **README 口径同步完成**：`build.md` 的"README 待同步项"消除——README.md / README_zh.md 域工具口径 ~366 工具 / 27 类与 `overview.md`"366 域工具"一致，`overview.md` 的 MCP 可达 373 / catalog 374 为含 7 元工具与 `system_status` 的另一口径；`--package` / `--debug` 在 README 构建章节未提及的观察经复核仍属实、保留。

## 2026-09-13-20

- **主 skill（GDA 使用指南）增强——必读定位与四块内容**：`src/util/skill_templates/registry.json` 中 `godot-autopilot` 的 description 改为以"使用 GDA 插件前必读"开头，其后概括连接前置、搜索优先的工具发现、引擎版本文档查询、引擎状态观测、任务到工具路由、错误水印协议，保留 "Use for anything godot-autopilot related in a Godot project." 结尾（长度 ≤1024）。
- **`src/util/skill_templates/autopilot.md` 正文**：intro 增加必读声明；Discovery protocol 追加 "Search techniques"（按对象/动词搜索、同义词重试、`category`/`tags` 过滤、`list_categories` 浏览、`get_tool_detail` 读 schema 与副作用标记、BM25 排序受措辞影响、无果查 references/tool-catalog.md、永不猜工具名）；新增 "Consult the engine documentation"（查文档时机；文档工具直读运行中引擎自带的离线文档缓存、随引擎版本匹配，禁止凭记忆使用引擎 API）；Task routing 前置 "Choosing the right tool" 决策清单（改属性→`property_set` 并回读、查属性→`property_get`/`property_get_list`、瓦片→`set_tilemap_cell`/`set_tilemap_cells` 与 tileset 工具、节点/资源/脚本/文档/运行时/日志各归其工具、批量→`batch_execute`、循环与数学→`code_execute`）；新增 "Watch the engine state"（写操作后先读 `new_errors_since_last_call`，非 0 先取编辑器日志/调试器/插件日志真实错误行再继续，不连续盲操作）。既有章节语义与顺序不变，新正文反引号词仅复用既有工具名/参数名/白名单词。
- **知识库同步**：`docs/wiki/modules/support.md` 的 8 册清单中 `godot-autopilot` 条目补必读定位与四块新增内容（其余 7 册描述不动），frontmatter `timestamp` 与审计日期注同步；本文件与 `log.md` 追加本条。反引号词表校验由主代理统一运行 L1 验证。

## 2026-09-13-21

- **skill 统一纯英文**：`src/util/skill_templates/registry.json` 主册 `godot-autopilot` 的 description 改为英文必读定位（"Required reading before using the GDA (godot-autopilot) plugin"，其后概括连接前置、搜索优先的工具发现、引擎版本文档查询、引擎状态观测、任务到工具路由与错误水印协议），其余 7 册 description 与全部字段不动，JSON 合法性经解析验证。
- **`src/util/skill_templates/scene-system.md`**：Gotchas 首条中引用的中文错误提示原文改为英文转述，保留"无法解析的路径报节点未找到错误、提示给出当前场景根与合法路径写法"的事实，不逐字引用中文。
- **扫描与知识库同步**：全库扫描确认 `src/util/skill_templates/` 其余文件无中文字符；`docs/wiki/modules/support.md` 同步（8 册清单主册条目 description 引述更新为英文并注明 skills 内容与 description 统一纯英文、审计日期注追加 21 时项、frontmatter `timestamp` 更新为 2026-09-13T21:13:00+08:00）。
