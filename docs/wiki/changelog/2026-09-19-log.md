# 2026-09-19 维护日志

## 2026/09/19 03:31

- **ToolSpec 数据化重构（全量落地，未提交）**：工具声明从"宏生成真类 + 独立 schema 文件"整体迁移为数据记录 `ToolSpec`，按批次落地如下。
  - **数据层**：新增 `src/tools/tool_spec.hpp`——`ToolSpec{name, description, category, tags, side_effect, flags, params, handler, raw_schema}` + `ParamSpec`（= `schema::ParamDef`）+ `tool_flags` 位标志（kMeta/kDynamic/kMutating/kObserve/kCaptureImage/kSceneTarget/kUndoable）+ `SpecTool : ToolBase, ISideEffect` + `make_spec_tool`；schema 由参数表经 `schema::build_schema` 派生、`raw_schema` 非空时优先。
  - **执行管线**：新增 `src/tools/tool_pipeline.{hpp,cpp}`——`SpecTool::execute()` 固定授权门（`deny_if_unauthorized`）→ handler → `pipeline::run_post`（`kObserve` 且 `observe=true` 时合并编辑器截图）；Input 域 4 个合成输入工具收编，`input_click_ops.cpp` 的重复后处理实现已删。新增 `src/tools/tool_args.{hpp,cpp}`（`Args` 取参器：opt_/require_/get_ + `reject_unknown`）。
  - **试点域 → 全量 30 域迁移**：30 个域 391 个工具 + `system_status` + 7 个元工具全部改为 `SpecTool`（元工具 `flags = kMeta`，其中 search_tools/batch_execute/code_execute 手写 schema 存 `raw_schema`）；删除 `GDA_TOOL_CLASS`/`GDA_TOOL_CLASS_SIDE` 宏（`tool_decl.hpp`）、`fn_tool.hpp`/`FnTool`、`meta_tools.hpp`/`MetaTool`/`IMetaTool`、8 个 `schema_*_ops.cpp`、`schema_fills.hpp`、`tool_input_schema`/`build_schema_for`。
  - **注册链路**：`register_all.{hpp,cpp}` 新增 `refresh_derived`（派生 catalog/BM25/dispatch handlers）与 `refresh_dynamic_tools`；`ToolRegistry::add()` 改按 `flags & kMeta` 归类；`get_tool_detail` 回显 `side_effect`/`dynamic`/`mutating`；catalog/index 399 = 391 域 + `system_status` + 7 元，MCP 恒 7 元、经 `call_tool` 可达 398。
  - **用户脚本 API**：新增 `src/tools/autopilot_tools.{hpp,cpp}`（`AutopilotTools` 单例：register_tool/unregister_tool/has_tool/list_tools/get_tool/call_tool/is_enabled/set_enabled）+ `dynamic_spec_store.hpp`；动态工具能力 `user_tools` 授权门（env `GODOT_AUTOPILOT_ALLOW` 优先于配置 `allow`，dock 复选框 "Allow user tools"）；L2 新增 `tests/config/26_user_tools_code_mode.json`（注册 → MCP 调用 → 发现性 → code-mode 直连 → 注销）。
  - **测试与守卫**：新增 `tests/unit/tool_args_test.cpp`（11 项）与 `tool_registry_test` 的 flags 路由用例；新增 `tests/guard/migration_guard.cmake` + `migrated_domains.txt`（30 域 + strict，实测输出 `OK (domains: 30, strict: on)`）；`tests/runner/traversal.cpp` 改为运行时经 `search_tools` 空 query 枚举（392 条）后按 `side_effect`/`mutating`/`dynamic` 排除，330 个进入空参契约 + 启发式冒烟两步骤（实测全过）。
  - **B9 收尾**：删除诊断用例 `tests/config/99_user_tools_diag.json`。
- **文档终态同步（本批次）**：`AGENTS.md`（架构/添加工具/测试/知识局限/构建行号段）、`docs/wiki/tool_base_design.md`（整体重写为 ToolSpec 设计页，保留文件路径）、`docs/wiki/modules/tools_registry.md`（注册链路/三层结构/元工具/动态工具/契约缺口/遍历机制/类别分布重写）、`docs/wiki/conventions.md`（添加工具流程改为参数表 + `make_spec_tool` 一行）、`docs/wiki/tests.md`（L1 255 / L2 27 / ctest 283 与迁移守卫、运行时遍历）、`docs/wiki/overview.md`、`docs/wiki/index.md`、`docs/wiki/security_contract.md`（`user_tools` 能力与 `ToolSpec` 标记口径）、`docs/wiki/modules/tools_ops_a.md`（196 工具）、`docs/wiki/modules/tools_ops_b.md`（195 工具）、`docs/wiki/build.md`、`docs/wiki/plans/roadmap.md`（注册点口径）；涉及旧宏/旧 schema 的删除条目以"已删"表述保留在设计与审计历史中。
- **本批计数依据（静态核实，未运行构建）**：L1 255 = 26 个 unit 文件的 `TEST`/`TEST_F` 宏逐行统计（含新增 `tool_args_test` 11 项与 `tool_registry_test` 5→6）；L2 27 = `tests/config/*.json` 实测（00-10 共 11 份 + 11-19/20/22-26/28 共 16 份；21/27 号段空缺）；遍历排除 62 = `side_effect` 非空 60（writes_file 16 / writes_config 6 / shows_alert 4 / modifies_window 20 / process 6 / game_runtime 7 / code_execute 1）+ `side_effect=None` 但 `mutating` 2（`fill_tilemap_rect`/`build_nodes_from_spec`）；注册点 283 = 255 + 1 迁移守卫 + 27 L2（以 `ctest -N` 实测为准）。
- **时间真实性**：本节时间为真实时间 2026-09-19 03:31 +08:00；历史日志零改写。

## 2026/09/19 03:51

- **文档口径同步（未提交）**：`tests/README.md` 更新为当前实测口径——L1 = 255 gtest（26 文件）+ 迁移守卫 1 项 = ctest 过滤后 256 条；L2 = 27 份 `tests/config/*.json`（补齐 `20_cjk_text_roundtrip`/`26_user_tools_code_mode` 的 ctest 名与用例表项；21/27 号段空缺、99 号诊断用例已删），ctest 总注册点 283 = 256 + 27；`03_tools_contract` 遍历口径为运行时经 `search_tools` 空 query 枚举 392 条（391 域 + system_status、跳过 7 元）后按 `side_effect`/`mutating`/`dynamic` 排除 62、330 个进入两步骤；`26` 号授权前置补充 `code_execute` + `user_tools`（env `GODOT_AUTOPILOT_ALLOW` 优先于配置 `allow`，env 已设且不含 `user_tools`/`all` 时用例内 `set_enabled(true)` 无效）；新增 5.5 迁移守卫小节（`tests/guard/migration_guard.cmake` + `migrated_domains.txt`，30 域 + `strict`，实测输出 `OK (domains: 30, strict: on)`）；"架构一句话"中残留的 `g_handlers` 直连描述改为 registry/`refresh_derived` 派生口径；不可静态核实的耗时类数字标注"以运行时统计为准"。
- **wiki 补充（未提交）**：`docs/wiki/tool_base_design.md` 的"动态工具（用户脚本 API）"章节新增 C# 鸭子类型示例——`Engine.GetSingleton("AutopilotTools")` 返回 `GodotObject`，`Call("register_tool", definition, Callable.From<Dictionary, Dictionary>(probe.Invoke))` / `Call("call_tool", name, args)` / `Call("unregister_tool", handle)`，宿主为持久 `RefCounted` 派生对象，需 .NET 版编辑器且脚本先编译；明确标注该示例为文档级、未经 CI/自动化验证，GDScript 路径由 `tests/config/26_user_tools_code_mode.json` 端到端覆盖；同步 frontmatter `timestamp` 为 2026-09-19T03:51:17+08:00。
- **时间真实性**：本节时间为真实时间 2026-09-19 03:51 +08:00；历史日志零改写。

## 2026/09/19 04:05

- **Editor 域 Observe 收编（未提交）**：`click_editor_element` / `type_editor_element_text` 的 flags 由 `kMutating` 改为 `kMutating | kObserve`，`observe=true` 时的编辑器截图合并统一走 `pipeline::run_post`（与 Input 域口径一致）；`editor_ui_actions.cpp` 删除匿名命名空间内重复的 `merge_observe_result` 及两处调用，`observe` 参数校验保留并在其后 `(void)observe;` 防未使用告警，文件不再引用 `capture_ops.hpp`。自检：`merge_observe_result` 在 `src/` 仅剩 `tool_pipeline.cpp` 一处；`kObserve` 出现于 `input_tools.hpp` 4 处、`editor_tools.hpp` 2 处（另有 `tool_spec.hpp` 定义与 `tool_pipeline.cpp` 消费各 1 处）。
- **删除死字段 `ToolMeta::basic_schema`（未提交）**：`tool_base.hpp` 移除该字段、`tool_spec.hpp` 的 `ToolMeta` 聚合初始化同步去掉 `false`；全仓（`src/` + `tests/`）核查无其他初始化点或消费者；`tool_base_design.md` 删除 `basic_schema=false` 描述并补充 Editor 域 2 工具收编说明，frontmatter `timestamp` 同步。
- **时间真实性**：本节时间为真实时间 2026-09-19 04:05 +08:00；历史日志零改写。

## 2026/09/19 15:23

- **收尾第二轮文档同步 + Example 白名单（未提交，仅文档与 1 处配置）**：零代码改动（未编译、未运行 ctest/godot、未提交）。
  - **traits 收编终态**：`kCaptureImage` 9 工具（`register_all.cpp` 按 flag 判定图片附件单一来源，旧白名单 `is_image_capture_tool` 仅作未知工具兜底）/`kSceneTarget` 25 工具（`tool_pipeline.cpp::run_post` 成功响应缺 `scene_path` 时幂等补全，不做脏标记）/`kUndoable` 10 工具（纯标记）；`tool_invoke.{hpp,cpp}`（`invoke_tool`/`invoke_depth`，深度上限 8）/`AutopilotTools::rescan`（默认 `res://addons/godot-autopilot-tools`，256 文件/深度 8/逐文件隔离）/`batch_execute` 最小变量串联（`$prev`/`$steps[N].result`/`$steps[N].error`，`unresolvable reference` 前缀）均已落地，文档见 `AGENTS.md` 架构段与 `docs/wiki/tool_base_design.md` 新增三节。
  - **Example 白名单**：`Example/.gitignore` 追加 `!user_tools_samples/` + `*.gd`/`*.cs` 否定规则（裸 `*` 对子目录生效，故需目录与内容两层规则），`echo_tool.gd`/`echo_tool.cs` 经 `git add --dry-run` 与 `git status --porcelain`（`??`）验证可见；`.uid` 生成文件仍被忽略。
  - **计数同步**：L1 255→269（新增 `tool_pipeline_traits_test` 4 / `tool_invoke_test` 4 / `batch_refs_test` 6，unit 文件 26→29；`TEST`/`TEST_F` 宏复核 269）/ L2 27→28 份（新增 `27_user_tools_rescan`，config glob 复核 28；21 号段空缺）/ ctest 注册点 283→298（269 + 1 + 28，主代理全量实测 298/298 全绿）；`BUSINESS_SOURCES` 显式数 22→23（补 `tool_invoke.cpp`）；26/27 号授权前置统一为 `code_execute` + `user_tools`。同步页：`AGENTS.md`（架构/添加工具/测试三段）、`tool_base_design.md`（traits/invoke/rescan/batch）、`modules/tools_registry.md`（图片附件 flag 判定、rescan 入口）、`tests.md`（3 个 L1 文件 + 27 号 L2）、`tests/README.md`（计数 + 27 号行）。
  - **C# 诚实标注**：`echo_tool.cs` 与 C# 鸭子类型示例均为文档级（`dotnet 10.0.302` 本机可用，但仓库无 .NET 编译验证环节，未经本仓库 CI 验证）。
- **只读自检**：`cmake -DSOURCE_DIR=<仓库根> -P tests/guard/migration_guard.cmake` 输出 `OK (domains: 30, strict: on)`；`git check-ignore`（非 verbose）对两示例文件退出码 1（不再被忽略）。
- **时间真实性**：本节时间为真实时间 2026-09-19 15:23 +08:00；历史日志零改写。

## 2026/09/19 17:27

- **示例目录迁移（未提交，仅文档与示例位置）**：`Example/user_tools_samples/` → `samples/user-tools/`，原因：`Example/` 是 L2 端到端测试工程、示例属噪声；`Example/.gitignore` 白名单同步撤销（现无示例白名单条目）；引用路径已全仓更新。
- **历史记录恢复说明**：15:23 节 `!user_tools_samples/` 行与 `log.md` 2026-09-19 摘要 `user_tools_samples/` 行已恢复原貌（撤销上一轮顺手改动，历史日志零改写）；`tests.md` 尾句改为现事实表述。
- **时间真实性**：本节时间为真实时间 2026-09-19T17:27:49+08:00；历史日志零改写。

## 2026/09/19 18:05

- **删除**：执行 2026-08-12"不写注释、代码即文档"约定，5 个并行批次（文件互不相交）删除 `src/` 241 行整行 `//` + 约 5 处行内 `/*名字=*/` + `tests/` 236 行整行 `//` + 14 处行尾 `//`；`// namespace xxx` 结尾标记 500 处保留（实测不变）；`samples/`（教学示例，豁免）、`Example/`（测试工程，不动）、字符串字面量内数据（如 `godot://`、`res://`、`metadata/*`）零触碰。
- **移植**：有信息量的解释（引擎源码引用、协议取舍、测试意图）已移植进 wiki——`entry_runtime.md` 新增归档节 22 条、`tools_ops_a.md` 15 条、`tools_ops_b.md` 归档、`core.md`/`support.md`/`tests.md` 若干条；各页 frontmatter timestamp 已同步。
- **守卫**：新增防回退守卫 `tests/guard/comment_guard.py`（C++ 词法级扫描，raw string/字符字面量感知），`tests/CMakeLists.txt` 注册 `comment_guard` ctest；`AGENTS.md` 约定段追加注释规范一行。
- **验证**：构建 OK；L1 `ctest --preset debug -E "^gda_runner_"` 271/271 全绿（含转绿的 comment_guard）；`src`/`tests` 整行 `//` 实测归零。
- **时间真实性**：本节时间为真实时间 2026-09-19T18:05:04+08:00；历史日志零改写。
