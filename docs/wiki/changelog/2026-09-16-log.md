# 2026-09-16 维护日志

## 2026-09-16-00

- **失败修复批次文档收尾（feature/failure-remediation）**：同步根文档与知识库至本批次运行时实测口径——域工具 379→384（MCP 可达 386→391、catalog/index 387→392、非 SIDE 322→324、SIDE 57→60，其中 `fill_tilemap_rect` 以 `GDA_TOOL_CLASS_SIDE` 宏声明但 side_effect=None）、L1 172→229、L2 11→17 份、ctest 注册点 183→246。
- **新增 5 个工具入册**：`scene_tree_items` / `select_scene_tree_node`（编辑器场景树行枚举与选中，Editor 类 +2）、`fill_tilemap_rect`（矩形铺砖，>100000 格报错、单次 undo，TileMap 7→8）、`start_game_job` / `get_game_job`（长游戏脚本异步提交+轮询，job 表上限 16、过期宽限 2s，Game 10→12）。
- **行为变更入册**：8 项行为变更已并入对应功能页（modules/tools_ops_a / modules/tools_ops_b / modules/entry_runtime / tests / modules/support）——eval 编译错误 ≤2s 结构化返回与裸 result、game 工具超时预算链（上限 25000 + late_results ≤5 条）、batch_execute await_async、capture after_frames/when/scale（条件成立帧与抓拍帧相差约 1 帧，取证留滞后余量）、资源内联 `[sub_resource]`、写工具回显 scene_path/scene_unsaved、`godot://skills` 资源组、get_game_status 无参数。
- **根文档同步**：`AGENTS.md`（工具计数/新增工具/测试段）、`README.md` / `README_zh.md`（~379→~384，类别表 Editor 29→31、TileMap 7→8、Game 10→12）、`tests/README.md`（L1 229、L2 17 份清单、16_game_jobs 的 game_runtime 授权注明、SIDE 分类 60）。
- **知识库同步**：index / overview / tests / tool_base_design / conventions / build / plans/roadmap / modules/tools_registry（类别表修正 Input 15→23 笔误）/ modules/tools_ops_a（A 组 193，editor_ui_ops +2）/ modules/tools_ops_b（B 组 191，runtime 12 工具、tilemap 5 工具、超时预算链、await_async、capture 帧定时）/ modules/support（skills 资源组）/ modules/entry_runtime（eval 结构化错误与裸 result、超时预算常量）。
- **测试计数依据**：L1 229 为 `tests/unit/*_test.cpp` 的 `TEST`/`TEST_F` 宏逐行统计（新增 editor_ui_tree 6 / inline_resource_json 12 / skill_resources 12 / tilemap_ops 13，editor_coords 32→37、runtime_ops 3→12）；L2 17 份新增 11_editor_tree / 12_capture_params / 13_inline_subresource / 14_tilemap_rect / 15_scene_path / 16_game_jobs（步骤数由各 JSON 实数）；03 遍历 324 个非 SIDE 工具 0 失败、warnings 仍为基线 4 条；L2 授权 capability 由 `GODOT_AUTOPILOT_ALLOW` env 或 `user://godot_autopilot/config.json` 的 `allow` 字段提供（env 优先）。

## 2026-09-16-02

- **cacheable 结果声明 2026 规范缓存提示（feature/mcp-cache-hints）**：`server_context.cpp` 经 `ServerOptions::cache_hints` 为六个 cacheable 方法（`server/discover`、`tools/list`、`prompts/list`、`resources/list`、`resources/templates/list`、`resources/read`）统一声明 `ttlMs=0` + `cacheScope="private"`。动机：经 `server/discover` 协商到 2026-07-28 era 的客户端（实测 zcode）把 `ttlMs`/`cacheScope` 当 result 必填字段做 zod 校验，缺失即拒绝整个 `tools/list` 响应；走 initialize 握手的客户端（claude code / opencode / codex 等）协商到 2025-03-26 era 不受影响。`ttlMs=0` 语义为"立即过期"，客户端仍每次重新拉取，运行行为与未声明时一致（无回归）。实测（curl 双 era 探针）：2026 era 响应顶层出现 `ttlMs:0`/`cacheScope:"private"`，2025 era 为嵌套 `cacheHint:{ttlMs:0,cacheScope:"private"}`；L1 229/229、L2 17/17 全绿。

## 2026-09-16-03

- **客户端一键配置 8→20 扩容（feature/mcp-cache-hints 延续批次）**：调研 14 个候选客户端的项目级 MCP 配置支持（3 组并行子代理，官方文档+源码交叉验证），按"可能不生效即不支持"原则淘汰 Cline（`.cline/mcp.json` 仅官方 CLI 文档化、VS Code 扩展未确认读取）与 Antigravity CLI（官方文档支持但 CLI v1.0.0 存在项目级 "read but ignored" 已知 bug），其余 12 个全部纳入：ZCode（`.zcode/config.json`，`mcp.servers` 双层嵌套，`type=http` 而非桌面端 legacy `remote`）、pi（`.pi/mcp.json`，需 pi-mcp-adapter）、Command Code 与 CodeBuddy（与 Claude Code 共用 `.mcp.json` 同一条目 `type=http`）、Kilo Code（`.kilo/mcp.json` 旧格式 `streamable-http`，新核心自动迁移）、Roo Code（`.roo/mcp.json`，URL 型省略 type 会报错故必写）、Grok Build（`.grok/config.toml`，与 Codex TOML 同构）、Kimi Code（`.kimi-code/mcp.json`，`url`+`enabled` 无 type）、Zed（`.zed/settings.json`，`context_servers` 键、无 type，merge 保留用户其他设置）、Crush（`.crush.json`，顶层 `mcp` 键 + `$schema`，strict schema 不写 enabled）、GitHub Copilot VS Code 形态（`.vscode/mcp.json`，顶层键是 `servers` 而非 `mcpServers`）、Reasonix（`reasonix.toml`，`[[plugins]]` TOML 数组）。
- **实现**：`client_config_gen.hpp/cpp` — ClientId 枚举 8→20、`top_key()` 五种顶层键（mcpServers/mcp/servers/context_servers/ZCode 双层）、`uses_type_field`/`type_value`（remote/http/streamable-http/无）/`uses_enabled_field` 三维度差异化、`uses_toml(id)` 判定、`merge_toml_config` 增加 ClientId 参数（Codex/Grok `[mcp_servers` 段与 Reasonix `name = "..."` 两种 marker）；`mcp_config_dock.cpp` TOML 分支改用 `uses_toml` 覆盖三类 TOML 客户端。
- **测试与文档**：`client_config_gen_test` 11→20 用例（20 客户端路径断言、type/enabled 矩阵、ZCode 双层嵌套 merge、Crush `$schema`、共享 `.mcp.json` 三客户端同构、VS Code `servers` 键、Reasonix 追加与已配置检测）；L1 229→238、ctest 246→255 全绿。同步 support.md（表格 20 行+淘汰说明）/tests.md/overview.md/AGENTS.md 计数。
