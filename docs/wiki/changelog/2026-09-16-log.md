# 2026-09-16 维护日志

## 2026-09-16-00

- **失败修复批次文档收尾（feature/failure-remediation）**：同步根文档与知识库至本批次运行时实测口径——域工具 379→384（MCP 可达 386→391、catalog/index 387→392、非 SIDE 322→324、SIDE 57→60，其中 `fill_tilemap_rect` 以 `GDA_TOOL_CLASS_SIDE` 宏声明但 side_effect=None）、L1 172→229、L2 11→17 份、ctest 注册点 183→246。
- **新增 5 个工具入册**：`scene_tree_items` / `select_scene_tree_node`（编辑器场景树行枚举与选中，Editor 类 +2）、`fill_tilemap_rect`（矩形铺砖，>100000 格报错、单次 undo，TileMap 7→8）、`start_game_job` / `get_game_job`（长游戏脚本异步提交+轮询，job 表上限 16、过期宽限 2s，Game 10→12）。
- **行为变更入册**：8 项行为变更已并入对应功能页（modules/tools_ops_a / modules/tools_ops_b / modules/entry_runtime / tests / modules/support）——eval 编译错误 ≤2s 结构化返回与裸 result、game 工具超时预算链（上限 25000 + late_results ≤5 条）、batch_execute await_async、capture after_frames/when/scale（条件成立帧与抓拍帧相差约 1 帧，取证留滞后余量）、资源内联 `[sub_resource]`、写工具回显 scene_path/scene_unsaved、`godot://skills` 资源组、get_game_status 无参数。
- **根文档同步**：`AGENTS.md`（工具计数/新增工具/测试段）、`README.md` / `README_zh.md`（~379→~384，类别表 Editor 29→31、TileMap 7→8、Game 10→12）、`tests/README.md`（L1 229、L2 17 份清单、16_game_jobs 的 game_runtime 授权注明、SIDE 分类 60）。
- **知识库同步**：index / overview / tests / tool_base_design / conventions / build / plans/roadmap / modules/tools_registry（类别表修正 Input 15→23 笔误）/ modules/tools_ops_a（A 组 193，editor_ui_ops +2）/ modules/tools_ops_b（B 组 191，runtime 12 工具、tilemap 5 工具、超时预算链、await_async、capture 帧定时）/ modules/support（skills 资源组）/ modules/entry_runtime（eval 结构化错误与裸 result、超时预算常量）。
- **测试计数依据**：L1 229 为 `tests/unit/*_test.cpp` 的 `TEST`/`TEST_F` 宏逐行统计（新增 editor_ui_tree 6 / inline_resource_json 12 / skill_resources 12 / tilemap_ops 13，editor_coords 32→37、runtime_ops 3→12）；L2 17 份新增 11_editor_tree / 12_capture_params / 13_inline_subresource / 14_tilemap_rect / 15_scene_path / 16_game_jobs（步骤数由各 JSON 实数）；03 遍历 324 个非 SIDE 工具 0 失败、warnings 仍为基线 4 条；L2 授权 capability 由 `GODOT_AUTOPILOT_ALLOW` env 或 `user://godot_autopilot/config.json` 的 `allow` 字段提供（env 优先）。

## 2026-09-16-02

- **cacheable 结果声明 2026 规范缓存提示（feature/mcp-cache-hints）**：`server_context.cpp` 经 `ServerOptions::cache_hints` 为六个 cacheable 方法（`server/discover`、`tools/list`、`prompts/list`、`resources/list`、`resources/templates/list`、`resources/read`）统一声明 `ttlMs=0` + `cacheScope="private"`。动机：经 `server/discover` 协商到 2026-07-28 era 的客户端（实测 zcode）把 `ttlMs`/`cacheScope` 当 result 必填字段做 zod 校验，缺失即拒绝整个 `tools/list` 响应；走 initialize 握手的客户端（claude code / opencode / codex 等）协商到 2025-03-26 era 不受影响。`ttlMs=0` 语义为"立即过期"，客户端仍每次重新拉取，运行行为与未声明时一致（无回归）。实测（curl 双 era 探针）：2026 era 响应顶层出现 `ttlMs:0`/`cacheScope:"private"`，2025 era 为嵌套 `cacheHint:{ttlMs:0,cacheScope:"private"}`；L1 229/229、L2 17/17 全绿。

## 2026-09-16-03

- **客户端一键配置 8→20 扩容（feature/mcp-cache-hints 延续批次）**：调研 14 个候选客户端的项目级 MCP 配置支持（3 组并行子代理，官方文档+源码交叉验证），按"可能不生效即不支持"原则淘汰 Cline（`.cline/mcp.json` 仅官方 CLI 文档化、VS Code 扩展未确认读取）与 Antigravity CLI（官方文档支持但 CLI v1.0.0 存在项目级 "read but ignored" 已知 bug），其余 12 个全部纳入：ZCode（`.zcode/config.json`，`mcp.servers` 双层嵌套，`type=http` 而非桌面端 legacy `remote`）、pi（`.pi/mcp.json`，需 pi-mcp-adapter）、Command Code 与 CodeBuddy（与 Claude Code 共用 `.mcp.json` 同一条目 `type=http`）、Kilo Code（`.kilo/mcp.json` 旧格式 `streamable-http`，新核心自动迁移）、Roo Code（`.roo/mcp.json`，URL 型省略 type 会报错故必写）、Grok Build（`.grok/config.toml`，与 Codex TOML 同构）、Kimi Code（`.kimi-code/mcp.json`，`url`+`enabled` 无 type）、Zed（`.zed/settings.json`，`context_servers` 键、无 type，merge 保留用户其他设置）、Crush（`.crush.json`，顶层 `mcp` 键 + `$schema`，strict schema 不写 enabled）、GitHub Copilot VS Code 形态（`.vscode/mcp.json`，顶层键是 `servers` 而非 `mcpServers`）、Reasonix（`reasonix.toml`，`[[plugins]]` TOML 数组）。
- **实现**：`client_config_gen.hpp/cpp` — ClientId 枚举 8→20、`top_key()` 五种顶层键（mcpServers/mcp/servers/context_servers/ZCode 双层）、`uses_type_field`/`type_value`（remote/http/streamable-http/无）/`uses_enabled_field` 三维度差异化、`uses_toml(id)` 判定、`merge_toml_config` 增加 ClientId 参数（Codex/Grok `[mcp_servers` 段与 Reasonix `name = "..."` 两种 marker）；`mcp_config_dock.cpp` TOML 分支改用 `uses_toml` 覆盖三类 TOML 客户端。
- **测试与文档**：`client_config_gen_test` 11→20 用例（20 客户端路径断言、type/enabled 矩阵、ZCode 双层嵌套 merge、Crush `$schema`、共享 `.mcp.json` 三客户端同构、VS Code `servers` 键、Reasonix 追加与已配置检测）；L1 229→238、ctest 246→255 全绿。同步 support.md（表格 20 行+淘汰说明）/tests.md/overview.md/AGENTS.md 计数。

## 2026-09-16-14

- **视觉辅助场景设计批次（feature/vision-assist-scene，T7 文档同步）**：截图双侧新增 `annotate_nodes`（1-50 路径）+ `annotate_nodes_max`（1-50），蓝色编号框（编号独立于红色 UI 框），结果附 `node_elements[{id,path,type,ok,position,size,visible,behind?,error?}]`（最终图像素，与输入一一对应、单项失败隔离）与 200 总框预算的 `node_truncated`；编辑器侧解析编辑场景路径，游戏侧在游戏进程内解析绝对路径并经 `finalize_capture_response` 白名单透传。`diff_image:true`（editor 专用，须配 `diff_against_last`）多返 `diff_image_data` 高亮 `changed_bbox`，无框或不可比较时不返图。新只读组合 `review_scene_visually`（Capture 类）一次返回 `editor_capture`/`game_capture`/`nodes`/`mapping`，各节失败隔离，缺 `annotate_nodes` 时 `nodes={skipped:true}`，未知参数拒绝。
- **知识库同步**：`modules/tools_ops_b`（capture 表 1→2 工具、B 组 191→192、域工具 384→385、可达 392→393，游戏/截图两节增视觉辅助口径与 VLM 标准链路：截图选框 → `hit_test_editor_point` 复核 → 动作 → `diff_against_last` 复验）+ 本日志。`overview.md` 全局计数与 `src/util/skill_templates/` 技能提示链未动（前者待运行时统计复核，后者属 `src/` 需另权处理，由主代理跟进）。

## 2026-09-16-17

- **坐标换算/键名统一/脚本新鲜度/UID 守卫/CJK 往返/open 幂等/属性两轮批次知识库同步（工作区已暂存 22 文件，`git diff HEAD` 核对）**：`src/core/editor_coords` 新增 `viewport_point_to_window`/`viewport_rect_center_to_window` 纯函数（`screen_transform×canvas` 复合、恒等原样返回）；`game_bridge.cpp:run_ui_click` 经控件视口 `get_screen_transform()` 复合 `get_canvas_transform()` 换算后注入，响应新增 `viewport_position`/`window_position`（`position` 保留画布坐标向后兼容）；`game_bridge_input.cpp` 删 57 项本地键名表、改调 `input_map_ops::resolve_key_name_code`（归一化：大写→剥 `KEY_`→单字符 ASCII→裸名表+`RETURN`/`CONTROL` 别名→数字串≤0xFFFFFF），非法键名错误统一为可用形式说明；`input_map_ops.cpp` 同判定供 `add_input_map_action_event`（裸名/`KEY_` 前缀/大小写不敏感/数字码等价）；`script_ops.cpp` 新增 `script_load_cache_mode`/`wants_fresh_load`（只认字面 true）+ `load_script_fresh`（IGNORE 装载+`reload(true)`），`load/get_property/get_property_list` 新增 `fresh` 参数、`attach`/`reload` 恒重读、`create_script` 响应 `cache_invalidated`→`cache_refreshed`+`cache_refresh_error`，源码与诊断链路全 `String::utf8`；`text_ops.cpp` 入参/写盘全 `String::utf8`、`build_script_diagnostics` 改 IGNORE 装载；`resource_ops.cpp` 新增 `uid_needs_add` 按表成员选 `add_id`/`set_id`（`sync_editor_and_uid_after_save` 与 `handle_set_uid` 同走）；`editor_ops.cpp:handle_open_scene` 同路径经 `same_scene_path` 直接返 `{ok, path, already_open:true}`、正常打开亦回显 `path`/`already_open:false`；`scene_ops.cpp:handle_create` 属性两轮下发（`property_value_is_object_typed` 反射判定 OBJECT 型先、同轮字典序，`animation` 落 `sprite_frames` 后）。
- **测试计数依据（静态核算，未编译运行）**：L1 246→267（新增 `game_ui_coords_test` 6 / `keycode_alias_test` 6 / `script_freshness_test` 7 / `uid_guard_test` 2，`TEST`/`TEST_F` 宏逐行统计，unit 文件 21→25）；L2 18→25 份（新增 18_script_freshness 13 步 / 19_cjk_roundtrip 4 步 / 22_uid_guard 13 步 / 23_click_ui_coords 8 步 / 24_keycode_alias 13 步 / 25_open_scene_idempotent 9 步 / 28_sprite_frames_animation 10 步，步数为各 JSON `"tool":` 计数，20/21/26/27 号段空缺）；ctest 注册点 264→292（267+25）；03 遍历候选 324→325（`review_scene_visually` 计入）、上限 650 步，warnings 基线 4 条不变（本轮未动相关工具）；工具数 385/392/393、SIDE 60（325+60）复核无误（`GDA_TOOL_CLASS(` 325 / `GDA_TOOL_CLASS_SIDE(` 60）。
- **知识库同步**：index（速查 393/385/392、L1 267、L2 25、ctest 292）/ overview（审计行+L1/L2/ctest+目录树）/ tests（审计行、L1 表补 vision_assist 8 项 + 新增 4 文件 21 项、L2 表 +8 行、授权前置补 18/22 code_execute 与 23/24 game_runtime、遍历 325/上限 650、核算总表、遗留补 window_position e2e 待补测）/ conventions（384→385）/ modules/tools_registry（审计行、384→385/324→325/392→393/391→392 全量、Capture 1→2）/ modules/tools_ops_a（A 组 193/50.1%/catalog 393、scene 两轮、input_map 统一判定、resource UID 守卫、script fresh 口径、editor 幂等）/ modules/tools_ops_b（text CJK 全链路、click 窗口坐标、键名错误文案、385 域中 325 入枚举）/ modules/entry_runtime（桥接文件行数口径、run_ui_click 换算、parse_keycode 删表）/ modules/core（补 `editor_coords` 职责行与新增纯函数、9 cpp + 12 头 + 模板、审计行）/ AGENTS.md（工具计数/新增批次/测试结构/L1 全绿/遍历口径）；各页 `timestamp` 同步真实时间 2026-09-16T17:06:25+08:00，带审计日期头页面同步日期。
- **遗留与风险**：L1/L2/ctest 计数为静态核算（宏逐行 + JSON 计数 + 声明计数），未执行 `cmake --build` 与 `ctest`，运行时统计（schema 空/非空、`03_tools_contract` 实际步数与 warnings、`review_scene_visually` 计入后首轮遍历）待构建运行后复核；`click_game_ui_element` 的 `window_position` 端到端断言待示例游戏恢复后补测（骨架见 23_click_ui_coords.json 覆盖力声明）；`core.md` 职责表此前长期漏列 `editor_coords`（本次一并补上，另修正 8 cpp/20 文件→9 cpp + 12 头 + 模板的陈旧口径）。
- **0.2.5 升版收尾同步**：根 `VERSION` 与 `AGENTS.md` 早在 09-14 已为 0.2.5（`GDA_VERSION "0.2.5"` 注入已生效，`build/debug/generated/version.hpp` 实测一致，`VERSION` 为 CRLF），本次仅补齐遗漏页——`build.md` 的 release validate 示例 `v0.2.4` ↔ `0.2.4`（当前 0.2.4）→ `v0.2.5` ↔ `0.2.5`（当前 0.2.5）+ 审计行与 `timestamp`（2026-09-16T17:20:00+08:00）；全库复核确认无其他滞留 0.2.4 现行版表述（changelog/report 内历史记录除外）。
