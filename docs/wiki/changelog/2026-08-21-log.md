# 2026-08-21 维护日志

## 2026-08-21-12

**遍历副作用排除改为 `side_effects()` 驱动，删除 runner 硬编码清单（L1 78/78 + L2 6/6 通过）**：

- `SideEffect` 枚举扩为 6 值（追加 `Process`）；新增 `inline side_effect_name(SideEffect)` 返回字符串。
- `tool_decl.hpp` 新增变体宏 `GDA_TOOL_CLASS_SIDE(..., SideEff)`（`ToolBase + ISideEffect`，`side_effects()` 返回 SideEff）。
- 给 **35 个**副作用工具改用 `GDA_TOOL_CLASS_SIDE` 并标记：WritesConfig（set_editor_main_scene/set_editor_plugin_enabled/save_project_settings/set_editor_settings/save_input_map/add_input_map_action_event）、WritesFile（save_editor_scene*3/save_editor_scene_as/write_file/move_os_file_to_trash/create_script/save_resource）、ShowsAlert（show_os_alert/show_display_dialog/speak|stop_display_tts）、ModifiesWindow（15 个 display_window/mouse/clipboard）、Process（build_csharp_assembly/create|execute|kill_os_process/set_os_environment/open_os_path）。
- `meta_get_tool_detail_impl` 经 `g_active_registry->find_any` + `side_effect_of` + `side_effect_name` 在 `get_tool_detail` 的 `tool` 对象补 `side_effect` 字段；`g_active_registry` 定义前移至该函数之前（修声明顺序）。
- `tests/runner/traversal.cpp`：删除 `kExcludedSideEffectTools` 数组与 `is_excluded_tool()`；循环改为先取 `get_tool_detail`、读 `side_effect`、非空即排除，再走原前置校验+执行。
- 验证：全量构建 + ctest **L1 78/78 + L2 6/6 = 84/84**（含 03 全遍历，副作用工具正确排除）。

## 2026-08-21-11

**元工具改为接口 + 组合（实现 `IMetaTool` 即元工具）**：

- `tool_base.hpp` 新增标记接口 `IMetaTool`（空虚析构，供 `dynamic_cast` 识别）。
- 新增 `src/tools/meta_tools.hpp`：`class MetaTool : public ToolBase, public IMetaTool`，构造注入 `(ToolMeta, HandlerFn, schema)`，`execute` 调 handler、`input_schema` 返回组合的 schema——依赖（index/catalog/处理逻辑）全经构造函数组合。
- `ToolRegistry::add()` 改为按 `dynamic_cast<const IMetaTool*>` 自动路由：实现接口的进 meta_、否则进 tools_（**实现接口即元工具**，Java 心智）；`add_meta()` 保留直写 meta_ 兼容测试。
- `register_all.cpp`：7 个元工具（ping/search_tools/list_categories/get_tool_detail/call_tool/batch_execute/code_execute）由 `add_meta(FnTool(...,false))` 改为 `add(MetaTool(...))`（handler lambda 与 schema 逐字保留，仅换类/方法）。
- 新增 L1 `ToolRegistryTest.AddRoutsMetaToolByInterface`（MetaTool 经 add() 自动进 find_meta；普通 FnTool 不进）。

## 2026-08-21-00

**ToolBase 工具统一标准化全量落地：336 域工具悉数真类化 + ToolRegistry 单一来源，`tool_defs.def` 删除（L1 77/77 + L2 6/6 通过）**：

- **真类基础设施**：新增 `src/tools/tool_decl.hpp`（宏 `GDA_TOOL_CLASS(ClassName, ToolName, Desc, Category, TagsList, ExecFn, BasicSchema)` 生成委托既有 handler 的独立 `ToolBase` 子类；`input_schema` 统一经 `tool_input_schema` 取）；`register_all.hpp/cpp` 暴露 `mcp::JsonValue tool_input_schema(name, basic)`（转发 `build_schema_for`，与旧 def 路径零漂移）。
- **336 域工具悉数真类化**：新增 26 个 `src/tools/<域>_tools.hpp`（每个含类 + `make_tools()`），覆盖 audio/capture/config/debug/debugger/doc/display/editor/game/group/input/input_map/log(none)/nav/os/physics/property/render/resource/scene/scene_tree/script/spriteframes/system/text/tilemap/tileset。
- **分类边界**：`get_debug_object_info`（def 标 Debug、handler physics_ops）归 `physics_tools`；`read_file/find_in_files/write_file`（def 归 OS、handler text_ops）归 `os_tools`；Scene Tree 类工具 Category="Scene" 按 handler 分 `scene_tools(4)`/`scene_tree_tools(8)`；InputMap 工具 Category="Input" 归 `input_map_tools`；`get_game_log_entries`（Category="System"、handler log_ops）归 `system_tools`。
- **注册重构**：`tool_defs.def` **已删除**；`register_all` 改为 add `system_status`(FnTool) → 逐一注册 26 域 `make_tools()`（336 真类）→ `add_meta` 7 元工具(FnTool)；catalog(344)/BM25 index/g_handlers/g_meta_handlers/RegisterTool 全部从 registry 派生。
- **遍历 runner 迁移**：`tests/runner/traversal.cpp` 的 `parse_domain_tool_names()` 由解析 `tool_defs.def` 改为遍历 `src/tools/*_tools.hpp` 解析 `GDA_TOOL_CLASS(` 第 2 参数，`std::filesystem` 枚举（`_tools.hpp` 尾缀 10 字符，修正 `compare` 偏移 11→10 的 bug）。
- **工程修坑**：① `GDA_TOOL_CLASS` TagsList 实参必须用圆括号 `std::vector<std::string>({...})`（花括号逗号被预处理器拆散 → "too many arguments"）；② `make_tools()` 必须 `push_back(std::make_unique<Class>())`（vector 花括号 initializer_list 触发 unique_ptr 拷贝已删除）；③ `os_tools.hpp` 补显式 `#include "tools/os_ops.hpp"`；④ `std::string::ends_with` 为 C++20，改用 `compare(size-10,10,"_tools.hpp")==0`。
- **验证**：全量构建通过（dll 约 20.5MB）；**L1 77/77 + L2 6/6**（00_meta/01_scene/02_property/03_tools_contract 全遍历 **546 步**/04_resources_scripts/05_rename_references）；`RegistryIsSingleSourceOfTools(root)` 断言 meta==7、all_any()==catalog(344)。
- **未落地**：`side_effects()` 驱动遍历排除（runner 仍用硬编码 kExcludedSideEffectTools 清单）；元工具仍为 FnTool 形式（未改独立真类）。
- **文档**：本日 changelog + `tool_base_design.md`（状态→全量真类化已实现）、`tools_registry.md`（注册流程/覆盖范围/审计日期/timestamp）、`index.md`、`tests.md`（遍历来源改为 `*_tools.hpp`、步数 546）同步；`AGENTS.md` 的 `tool_defs.def` 引用与计数待主代理后续统一。