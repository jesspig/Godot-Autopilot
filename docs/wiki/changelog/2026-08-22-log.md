# 2026-08-22 维护日志

## 2026-08-22-01

**项目版本号 0.1.0 → 0.2.0，并收敛为根 `VERSION` 文件单一来源（构建 + L1 78/78 通过）**：

- 新增根目录 `VERSION` 文件（内容 `0.2.0`）作为版本号唯一真源。
- `CMakeLists.txt`：`project()` 前 `file(READ)` + `string(STRIP)` 读入；`configure_file(src/core/version.hpp.in → <build>/generated/version.hpp @ONLY)` 生成 `GDA_VERSION` 宏；`godot-autopilot` 与 `gda_unit_tests` 两个 target 追加 `${CMAKE_BINARY_DIR}/generated` include 路径。
- C++ 引用改造：`server_context.cpp` MCP `server_info`、`register_all.cpp` `system_status.version` 由硬编码字符串改为 `<version.hpp>` 的 `GDA_VERSION` 宏。
- `build.py`：`ADDON_VERSION` 改为运行时读取 `VERSION` 文件。
- 升版流程简化为：只改 `VERSION` 文件 + 重新 configure 即全量生效。
- 文档同步：`build.md` 新增"版本号单一来源"小节、产物名泛化为 `<version>` 占位符（含审计日期/timestamp）；`core.md` MCP 标识行改为宏引用描述；`log.md` 摘要联动。
- 验证：`uv run build.py` 全量构建部署通过（生成 version.hpp 内容正确）；`gda_unit_tests` L1 78/78 通过；`build.ADDON_VERSION` 读值断言 `0.2.0`。

## 2026-08-22-06

**大规模死代码清理与跨域去重（feature/cpp-cleanup-deadcode-dedup，56 文件、净删约 1400 行；构建通过，L1 71/71 + L2 77/77 全过，Unity batch=8 生效）**：

- **死代码删除**：`tool_base.hpp` 删 ArgReader/IExportGuard/IAsync/make_ok/make_error/blocks_export/tool_is_async；`fn_tool.hpp` 构造收缩为 (meta, handler, schema, SideEffect=None)（保留 ISideEffect）；`tool_registry.hpp` 删 add_meta/to_tool_info_all；resource_registry 删 is_registered；server_context 删 get_queue/start_time_；main.cpp 删 get_server_ctx；gda_protocol 删 GDA_READY_WAIT_MS/GDA_PLAY_READY_WAIT_MS。
- **测试瘦身**：log_system 回调用例删除；schema_builder 删 make_object_schema/make_empty_schema/add_param 等六胞胎，API 收敛为 build_schema/add_required_flag/ParamDef 三件；bm25_index 删字符串 search 重载；gtest 数 L1 78→71、全量 84→77。
- **公共设施新建（全 header-only）**：`util/json_godot.hpp`（json_number/json_to_vec2·3/rect2/color 与反向序列化、rid 互转）、`util/rid_registry.hpp`（RidStore + 模板标签 rid_store<Domain>/resolve_rid<Domain>）、`util/type_hint.hpp`（infer_type_hint）、`util/gdscript_wrap.hpp`（GDScript 包装流水线共享）。
- **跨域去重**：7 份 find_node 归一为 util::resolve_scene_node（tilemap 域顺带获得 root/ 前缀剥离——行为修复）；RidStore 双副本归一（physics/text 独立标签空间）；ok_json 四副本→util::ok_result；数值三元式约 80 处→util::json_number；script/code_exec 共享 GDScript wrapper；load_resource_or_error 六点收敛；editor undo_redo 合并；主屏切换助手。
- **register_all/dispatch**：删 25 个冗余 include 与 build_schema_for_none_by_name（tool_input_schema 的 basic 参数成为 no-op 但签名保留）；dispatch 删 meta_tool_names 硬编码名单改由 g_meta_handlers 派生；元工具 schema 五个转换 build_schema（search_tools/batch_execute/code_execute 因嵌套结构保留手写）；meta_batch_execute_impl/meta_code_execute_impl 内联；runtime_ops wait_for_response 更名 wait_pending_response。
- **缺陷修复**：register_all_tools 入口补 index.clear()（修复配置面板 restart 后 BM25 索引翻倍）；ReRegisterIsIdempotentForServerAndCatalog 增加 index.size 断言护航。
- **构建脚本**：根 CMakeLists 接线 set_target_properties(UNITY_BUILD/UNITY_BUILD_BATCH_SIZE)——Unity 构建首次实际生效（实测 batch=8/16 核）；cmake/Lto.cmake 删四处 GDA_LTO 死缓存变量；tests/CMakeLists.txt 三中间变量内联。
- **其他卫生**：client_config_gen 表驱动；game_bridge_input parse_keycode 表驱动化 + dispatch_input_event 事件工厂；main.cpp 四组 try/catch 提取 log_setup_failure；K5 卫生包（冗余 static、PROPERTY_USAGE_* 替代魔数 456、空析构删除、debugger_access.hpp 注释移除——依赖方向约束已落入 tools_registry 页）。
- 文档同步：AGENTS.md（add_meta 兼容措辞、gtest 数）、tools_registry/core/entry_runtime/build/overview/conventions/tool_base_design 七页与 log 摘要联动。
